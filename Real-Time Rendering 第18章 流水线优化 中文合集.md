# Real-Time Rendering 第18章 流水线优化 中文合集

> 依据用户提供的《Real-Time Rendering》第四版 PDF 完整翻译。原图配中文图注；复杂数学已排版为本地图片，简单符号直接显示。保留原书技术年代、公式编号与引用编号。

## 目录

- [18.1 性能分析与调试工具](<Real-Time_Rendering_4th_中文/第18章/18.01.md>)

- [18.2 定位瓶颈](<Real-Time_Rendering_4th_中文/第18章/18.02.md>)

- [18.3 性能测量](<Real-Time_Rendering_4th_中文/第18章/18.03.md>)

- [18.4 优化](<Real-Time_Rendering_4th_中文/第18章/18.04.md>)

- [18.5 多处理](<Real-Time_Rendering_4th_中文/第18章/18.05.md>)


## 章首导言

来源：原书书页 783—784（PDF 第 804—805 页），包括章标题、题辞及 18.1 节之前的全部导言。

> “我们应当忘掉那些微小的效率提升，比如说，在大约 97% 的时间里都应如此：过早优化是万恶之源。”
>
> ——唐纳德·克努特（Donald Knuth）

本书始终在权衡质量、内存和性能的背景下介绍各种算法。本章将讨论不依附于特定算法的性能问题和优化机会。重点是瓶颈检测与优化：从小范围的局部修改开始，最后介绍如何从整体上组织应用程序，以利用多处理能力。

正如第 2 章所述，渲染一幅图像的过程基于流水线架构，在概念上分为四个阶段：应用程序、几何处理、光栅化和像素处理。总有一个阶段是瓶颈，也就是流水线中最慢的处理环节。这意味着，这个瓶颈阶段决定了吞吐量的上限，也就是整体渲染性能的上限，因此它是优化的首要候选对象。

优化渲染流水线性能的过程类似于优化流水线处理器（CPU）[715]，主要包含两个步骤。首先，找出流水线的瓶颈。其次，以某种方式优化该阶段；此后，如果尚未达到性能目标，就重复第一步。注意，优化之后，瓶颈可能仍在原来的位置，也可能已经转移。一个好的做法是，对瓶颈阶段只投入足够的优化工作，使瓶颈转移到另一个阶段即可。在这个阶段再次成为瓶颈之前，可能还需要优化其他几个阶段。因此，不应把精力浪费在对某一阶段的过度优化上。

瓶颈的位置可能在一帧之内发生变化，甚至在一次绘制调用之内发生变化。某一时刻，由于正在渲染许多微小的三角形，几何阶段可能成为瓶颈。在这一帧稍后的时刻，由于每个像素都要计算一个开销很大的过程式着色器，像素处理又可能成为瓶颈。在像素着色器中，执行可能因纹理队列已满而停顿，也可能在运行到某个特定循环或分支时花费更多时间。因此，当我们说诸如应用程序阶段是瓶颈时，指的是在这一帧的大部分时间里，它是瓶颈。很少会只有一个瓶颈。

利用流水线结构的另一种方法，是认识到：当最慢的阶段无法进一步优化时，可以让其他阶段承担更多工作，直到它们所花的时间与最慢的阶段一样长。由于最慢阶段的速度没有改变，这不会改变性能，但额外的处理可以用来提高图像质量 [1824]。例如，假设瓶颈位于应用程序阶段，生成一帧需要 50 毫秒（ms），而其他阶段各自只需要 25 ms。这意味着，在不改变渲染流水线速度的情况下（50 ms 对应每秒 20 帧），几何阶段和光栅器阶段也可以各用 50 ms 完成工作。例如，可以使用更复杂的光照模型，或者通过阴影与反射提高真实感，前提是这样做不会增加应用程序阶段的工作负载。

计算着色器也改变了我们看待瓶颈和未利用资源的方式。例如，在渲染阴影贴图时，顶点着色器和像素着色器都很简单；如果光栅器或像素合并器之类的固定功能阶段成为瓶颈，GPU 的计算资源就可能未得到充分利用。在出现这些情况时，让此类绘制操作与异步计算着色器重叠执行，可以使着色器单元保持忙碌 [1884]。本章最后一节将讨论基于任务的多处理。

流水线优化的过程，是先将渲染速度提高到最大，再允许那些并非瓶颈的阶段消耗与瓶颈阶段相同的时间。不过，这个过程并不总是直截了当的，因为 GPU 和驱动程序可能各有其特殊行为与快速执行路径。阅读本章时，应当始终牢记这句格言：

> **了解你的架构。**

因为不同架构所适用的优化技术差异很大。不过，也要谨慎对待基于某一特定 GPU 对某项功能的实现方式所做的优化，因为硬件可能改变，而且一定会随时间推移而改变 [530]。与之相关的另一句格言则很简单：

> **测量，测量，再测量。**


## 18.1 性能分析与调试工具

来源：原书书页 784—785（PDF 第 805—806 页）。本节自 18.1 标题起，至 18.2 标题前止；已核对 PDF 第 807 页的节边界。

性能分析与调试工具对于找出代码中的性能问题可能具有极高的价值。不同工具的能力各不相同，可能包括：

- 帧捕获与可视化。通常可以逐步回放一帧，同时显示正在使用的状态和资源。
- 分析 CPU 和 GPU 各部分的耗时，包括调用图形 API 所花费的时间。
- 调试着色器，并且可能支持热编辑，以便查看修改代码后的效果。
- 使用应用程序中设置的调试标记，帮助识别代码的不同区域。

性能分析与调试工具会因操作系统、图形 API 而异，而且往往也因 GPU 厂商而异。大多数搭配都有相应的工具，这也就是诸神创造 Google 的原因。不过，为了让你开始探索，我们还是会具体提到几个面向交互式图形的工具包：

- **RenderDoc** 是 Windows 上的一款高质量调试器，支持 DirectX、OpenGL 和 Vulkan，最初由 Crytek 开发，如今已经开源。
- **GPU PerfStudio** 是 AMD 为其图形硬件产品提供的工具套件，可以在 Windows 和 Linux 上运行。其中一个值得关注的工具是静态着色器分析器，无须运行应用程序就能给出性能估计。AMD 的 Radeon GPU Profiler 则是一个独立但相关的工具。
- **NVIDIA Nsight** 是一个具有广泛功能的性能分析与调试系统。它在 Windows 上与 Visual Studio 集成，在 Mac OS 和 Linux 上与 Eclipse 集成。
- 微软的 **PIX** 长期以来一直为 Xbox 开发者所使用，如今又重新面向 Windows 上的 DirectX 12 推出。对于较早版本的 DirectX，可以使用 Visual Studio 的 **Graphics Diagnostics**。
- 微软的 **GPUView** 使用 Windows 事件跟踪（Event Tracing for Windows，ETW），这是一种高效的事件记录系统。GPUView 是使用 ETW 会话数据的多个程序之一。它着重分析 CPU 与 GPU 之间的交互，显示哪一方是瓶颈 [783]。
- **Graphics Performance Analyzers**（GPA，图形性能分析器）是英特尔提供的一套工具，并不局限于英特尔的图形芯片，主要用于性能分析和帧分析。
- OSX 上的 Xcode 提供了 **Instruments**，其中包含多种工具，用于计时、性能、网络、内存泄漏等方面。值得一提的有 **OpenGL ES Analysis**，它能够检测性能问题和正确性问题，并提出解决办法；还有 **Metal System Trace**，它提供来自应用程序、驱动程序和 GPU 的跟踪信息。

这些是已经存在了几年的主要工具。不过，有时没有任何工具能够胜任所需的工作。大多数 API 都内置了计时器查询调用，用来帮助分析 GPU 的性能。一些厂商还提供了程序库，用于访问 GPU 计数器和线程跟踪信息。


## 18.2 定位瓶颈

来源：原书第 786—788 页（PDF 第 807—809 页）。本节范围从 18.2 标题起，至 18.3 标题前，包含 18.2.1—18.2.5 全部小节。

优化流水线的第一步，是定位最大的瓶颈 [1679]。寻找瓶颈的一种方法是设计若干项测试，每项测试都减少某个特定阶段所执行的工作量。如果其中一项测试使每秒帧数上升，就找到了瓶颈所在的阶段。另一种相关的方法，是减少其他阶段的工作负载，同时保持被测试阶段的工作负载不变。如果性能没有变化，那么工作负载未改变的那个阶段就是瓶颈。性能分析工具能够详细指出哪些 API 调用开销较大，却不一定能准确指出流水线中究竟是哪个阶段拖慢了其他阶段。即使工具能够指出，理解每项测试背后的思路仍然很有帮助。

下面简要讨论测试各个阶段时采用的一些思路，让读者了解这类测试如何进行。统一着色器架构的出现，就是说明理解底层硬件有多么重要的一个绝佳例子。自 2006 年底起，许多 GPU 都以这种架构为基础。其思想是让顶点着色器、像素着色器及其他着色器使用同一组功能单元。GPU 负责负载均衡，调整分配给顶点着色与像素着色的单元比例。例如，渲染一个很大的四边形时，可能只需要给顶点变换分配少量着色器单元，而将大多数单元用于片元处理。这样一来，要准确判断瓶颈位于顶点着色器阶段还是像素着色器阶段，就不那么直观了 [1961]。不过，瓶颈仍然会是整体的着色器处理，或者某个其他阶段，因此我们将依次讨论各种可能性。

### 18.2.1 测试应用阶段

如果所使用的平台提供了测量处理器工作负载的实用工具，就可以用它检查程序是否占用了 100%（或接近 100%）的 CPU 处理能力。如果 CPU 始终处于忙碌状态，程序很可能受限于 CPU。不过，这种判断并非总是可靠，因为应用程序有时可能正在等待 GPU 完成一帧。我们虽然会说程序受限于 CPU 或 GPU，但在一帧的整个处理过程中，瓶颈可能发生变化。

测试 CPU 限制的一种更巧妙的方法，是向 GPU 提交只会让它执行很少工作、甚至不执行任何工作的数据。在某些系统上，只需用空驱动程序（接受调用却什么也不做的驱动程序）替代真正的驱动程序，就能实现这一点。这样实际上就确定了整个程序运行速度的上限，因为既不使用图形硬件，也不调用真正的驱动程序，因此运行在 CPU 上的应用部分始终是瓶颈。通过这项测试，可以了解那些不在应用阶段中运行、而是基于 GPU 的阶段还有多大的改进空间。不过需要注意，使用空驱动程序也可能掩盖由驱动程序本身的处理，以及 CPU 与 GPU 之间的通信所造成的瓶颈。驱动程序往往可能是 CPU 端瓶颈的成因，后面我们会深入讨论这一话题。

另一种更直接的方法，是在条件允许时降低 CPU 的时钟频率 [240]。如果性能随着 CPU 频率的降低而按比例下降，那么应用程序至少在一定程度上受限于 CPU。同样的降频方法也可用于 GPU。如果降低 GPU 速度后性能下降，那么应用程序至少在部分时间内受限于 GPU。这些降频方法有助于识别瓶颈，但有时也会使原本不是瓶颈的阶段变成瓶颈。另一个选择是超频，不过可别说你是在这里读到这个建议的。

### 18.2.2 测试几何处理阶段

几何阶段是最难测试的阶段。这是因为，改变这一阶段的工作负载时，其他两个阶段中的一个或两个的工作负载往往也会随之改变。为避免这一问题，Cebenoyan [240] 给出了一系列测试，从光栅化器的各阶段开始，沿流水线向上游逐步测试。

几何阶段主要有两个可能出现瓶颈的环节：顶点获取和顶点处理。要判断瓶颈是否来自物体数据传输，可以增大顶点格式的大小。例如，可以为每个顶点额外传送几组纹理坐标。如果性能下降，那么这个环节就是瓶颈。

顶点处理由顶点着色器完成。针对顶点着色器瓶颈的测试，是增加着色器程序的长度。这里需要小心，确保编译器不会把这些额外指令优化掉。

如果流水线还使用了几何着色器，那么它们的性能取决于输出大小和程序长度。如果使用了曲面细分着色器，程序长度同样会影响性能，曲面细分因子也会产生影响。在避免改变其他阶段工作量的前提下，调整上述任一因素，都有助于判断其中是否存在瓶颈。

### 18.2.3 测试光栅化阶段

这一阶段包括三角形设置和三角形遍历。阴影贴图生成使用极其简单的像素着色器，因此其瓶颈可能出现在光栅化器阶段或合并阶段。尽管通常很少见 [1961]，对于曲面细分产生的小三角形，或草、枝叶等物体中的小三角形，三角形设置和光栅化也有可能成为瓶颈。不过，小三角形也会增加顶点着色器和像素着色器的使用量。给定区域中的顶点更多，显然会增加顶点着色器的负载。像素着色器负载也会增加，因为每个三角形都以一组组 2 × 2 的像素块进行光栅化，所以落在各个三角形之外的像素数量会增加 [59]。这种现象有时称为四像素块过度着色（quad overshading，见第 23.1 节）。要判断光栅化是否确实是瓶颈，可以同时增大顶点着色器和像素着色器的程序长度，以增加它们的执行时间。如果每帧渲染时间没有增加，那么瓶颈就位于光栅化阶段。

### 18.2.4 测试像素处理阶段

可以通过改变屏幕分辨率，测试像素着色器程序的影响。如果降低屏幕分辨率后帧率明显上升，那么像素着色器很可能是瓶颈，至少在部分时间内如此。如果使用了细节层次系统，就需要小心：较小的屏幕也可能使显示的模型得到简化，从而减轻几何阶段的负载。

降低显示分辨率还会影响三角形遍历、深度测试与混合、纹理访问等因素的开销。为排除这些因素并单独定位瓶颈，可以采用与测试顶点着色器程序相同的方法：增加指令，观察它们对执行速度的影响。同样，务必确认编译器没有把这些额外指令优化掉。如果帧渲染时间增加，那么像素着色器就是瓶颈（或者至少在其执行开销增加的过程中，于某个时刻变成了瓶颈）。另一种做法是将像素着色器简化到只包含最少数量的指令，而这在顶点着色器中往往很难做到。如果总渲染时间减少，就找到了一个瓶颈。纹理缓存未命中也可能代价高昂。如果将某个纹理替换成分辨率为 1 × 1 的版本后，性能明显提高，那么纹理内存访问就是一个瓶颈。

着色器是独立的程序，有其自身的优化技术。Persson [1383, 1385] 介绍了若干底层着色器优化方法，还具体说明了图形硬件如何演进，以及最佳实践如何随之变化。

### 18.2.5 测试合并阶段

这一阶段执行深度测试和模板测试，进行混合，并将通过测试后保留下来的结果写入缓冲区。改变这些缓冲区的输出位深，是调整这一阶段带宽开销、观察它是否可能成为瓶颈的一种方法。为不透明物体开启 alpha 混合，或采用其他混合模式，也会影响光栅操作所执行的内存访问量和处理量。

在后处理通道、阴影、粒子系统渲染中，以及在较小程度上的毛发和草的渲染中，这一阶段都可能成为瓶颈，因为这些情况下的顶点着色器和像素着色器很简单，所做的工作很少。


## 18.3 性能测量

来源：原书第788—790页（PDF第809—811页）。本节从18.3标题开始，到18.4标题之前结束。

要进行优化，就必须进行测量。这里讨论衡量GPU速度的不同指标。图形硬件制造商过去常常公布每秒顶点数和每秒像素数之类的峰值速率，而这些数值即使在最理想的情况下也很难达到。此外，由于我们面对的是一个流水线系统，实际性能并不是列出这类数字那么简单。这是因为瓶颈的位置可能随时间改变，而不同的流水线阶段在执行期间也会以不同方式相互影响。由于这种复杂性，GPU的市场宣传在一定程度上依靠其物理规格，例如核心数量和时钟频率，以及显存容量、速度和带宽。

尽管如此，如果能够使用GPU计数器和线程跟踪，并且使用得当，它们就是重要的诊断工具。如果已知某个部件的峰值性能，而计数结果低于这一水平，那么这个部件就不太可能是瓶颈。有些厂商会将计数器数据表示为各阶段的利用率百分比。这些数值覆盖某一段时间，而瓶颈在这段时间里可能发生转移，因此它们并不完美，但对于找到瓶颈仍然很有帮助。

数值越高通常越好，但即使看起来很简单的物理指标，也可能难以精确比较。例如，同一种GPU在不同独立硬件厂商（IHV）的合作伙伴产品中，时钟频率可能有所不同，因为各家都有自己的散热方案，并会将GPU超频到自己认为安全的水平。即使在同一套系统上，比较以FPS表示的基准测试结果，也不总是听起来那么简单。NVIDIA的GPU Boost [1666]和AMD的PowerTune [31]技术，就是我们所说的“了解你的体系结构”这一准则的很好例子。NVIDIA推出GPU Boost的部分原因是，一些合成基准测试会同时让GPU流水线的许多部分工作，从而将功耗推到极限；这意味着NVIDIA不得不降低基础时钟频率，以免芯片过热。许多应用程序不会将流水线的所有部分都运用到这种程度，因此可以安全地以更高的时钟频率运行。GPU Boost技术会跟踪GPU的功耗和温度特征，并据此调整时钟频率。AMD和Intel也为其GPU提供了类似的功耗与性能优化。这种可变性会使同一个基准测试因GPU初始温度不同而以不同速度运行。为了避免这个问题，Microsoft在DirectX 12中提供了一种锁定GPU核心时钟频率的方法，以获得稳定的计时结果[121]。在其他API中也可以检查功耗状态，但会更加复杂[354]。

在衡量CPU性能时，人们已逐渐避免使用IPS（每秒指令数）、FLOPS（每秒浮点运算次数）、吉赫兹数以及简单短小的基准测试。相反，更受青睐的方法是，测量一组不同的实际程序所用的墙钟时间[715]，然后比较它们的运行时间。顺应这一趋势，大多数独立图形基准测试会针对若干给定场景，在各种屏幕分辨率以及抗锯齿和质量设置下，测量以FPS表示的实际帧率。许多对图形处理要求较高的游戏都内置基准测试模式，或者有第三方为其制作这种模式；这些基准测试常被用于比较GPU。

虽然在比较运行基准测试的GPU时，FPS是一种方便的简略指标，但在分析一系列帧率时，应避免使用它。FPS的问题在于，它是一种倒数形式的度量，而非线性度量，因此可能导致分析错误。例如，假设你发现应用程序在不同时刻的帧率分别为50、50和20 FPS。对这些值取平均会得到40 FPS。这个结果至少会造成误导。这些帧率分别对应20、20和50毫秒，因此平均帧时间为30毫秒，也就是33.3 FPS。同样，在测量单个算法的性能时，几乎必须使用毫秒。对于给定测试和给定机器上的某个特定基准测试情形，可以说某个阴影算法或后处理效果“消耗”了7 FPS，即基准测试的帧率降低了这么多。然而，将这种说法推广开来就没有意义，因为这个值还取决于处理该帧中其余所有内容所花的时间，而且不同技术的FPS不能直接相加（但时间可以相加）[1378]。

为了看清流水线优化可能带来的效果，测量每帧总渲染时间时，应禁用双缓冲，也就是关闭垂直同步，以单缓冲模式进行测量。这一点很重要，因为启用双缓冲后，缓冲区交换只会与显示器的刷新频率同步发生，第2.1节的例子对此作了解释。De Smedt [331]讨论了如何分析帧时间，以发现并修复由CPU工作负载突增引起的画面卡顿问题，同时还介绍了其他有用的性能优化技巧。通常有必要进行统计分析。也可以使用GPU时间戳，了解一帧内部发生的情况[1167, 1422]。

纯粹的运行速度固然重要，但对于移动设备，另一个目标是优化功耗。有意降低帧率，同时保持应用程序的交互性，可以显著延长电池续航时间，而对用户体验影响很小[1200]。Akenine-Möller和Johnsson [25, 840]指出，每瓦性能与每秒帧数类似，具有与FPS相同的缺点。他们认为，更有用的指标是每项任务消耗的焦耳数，例如每像素消耗的焦耳数。

> 译注：上文将关闭垂直同步、禁用双缓冲和单缓冲模式联系起来的表述，按原书保留。具体API与呈现机制中，这些设置并不普遍等价；关闭垂直同步本身不必然意味着改用单缓冲。


## 18.4 优化

来源：《Real-Time Rendering, Fourth Edition》书页 790—805（PDF 物理页 811—826）。本节从 18.4 标题开始，至 18.5 标题前结束；包含全部下级小节、图 18.1—18.7、公式（18.1）—（18.3）及两条脚注。技术陈述与性能数据均保留原书出版时语境。

一旦找到了瓶颈，我们就希望优化那个阶段来提高性能。本节介绍应用、几何、光栅化和像素处理阶段的优化技术。

### 18.4.1 应用阶段

优化应用阶段的方法是加快代码执行，以及加快或减少程序的内存访问。这里讨论一些普遍适用于 CPU 的代码优化要点。

对于代码优化，关键是找出代码中耗时最多的位置。优秀的代码性能分析器对于找到这些耗时最多的代码热点至关重要。随后便在这些位置进行优化。程序中的这类位置往往是内层循环，也就是每帧要执行很多次的代码片段。

优化的基本规则是尝试各种策略：重新审视算法、假设和代码语法，尽可能尝试不同变体。CPU 架构与编译器的表现，往往使人难以凭直觉判断怎样写出最快的代码，因此要质疑自己的假设，并保持开放的态度。

最初应做的事情之一，是试验编译器的优化选项。通常有许多不同选项可供尝试。对于应使用哪些优化选项，尽量少做预设，最好完全不做。例如，让编译器采用更激进的循环优化，反而可能使代码变慢。另外，如果条件允许，也应试试不同编译器，因为它们的优化方式不同，有些明显更优。性能分析器可以告诉你每项改变产生了什么影响。

#### 内存问题

多年前，算术指令的数量是衡量算法效率的关键；现在，关键在于内存访问模式。处理器速度的增长远快于 DRAM 数据传输速率的增长，后者受到引脚数量限制。1980—2005 年间，CPU 性能大约每两年翻一番，而 DRAM 性能大约每六年才翻一番 [1060]。这一问题称为冯·诺依曼瓶颈或内存墙。面向数据的设计把缓存连贯性作为优化的重点。注1

在现代 GPU 上，重要的是数据移动的距离。速度与功耗代价和这个距离成正比。缓存访问模式能够造成数量级的性能差异 [1206]。缓存是一小块快速存储区。它之所以有效，是因为程序通常具有很强的连贯性，可供缓存利用。也就是说，内存中相邻位置往往被接连访问（空间局部性），代码也常常按顺序访问。此外，同一内存位置往往会被反复访问（时间局部性），缓存也利用了这一点 [389]。处理器缓存的访问很快，速度仅次于寄存器。许多快速算法都致力于尽可能局部地访问数据，并尽可能减少访问量。

寄存器和本地缓存构成存储层次的一端，接下来依次是动态随机存取存储器（DRAM），再往后是 SSD 和硬盘存储。顶端是少量、快速而昂贵的存储，底端是大量、缓慢而廉价的存储。层次中每下降一级，速度都会按某个明显的倍数下降，见图 18.1。例如，处理器寄存器通常在一个时钟周期内即可访问，而 L1 缓存的访问需要几个周期。每次跨越层级，延迟都会这样增加。如第 3.10 节所述，架构有时可以隐藏延迟，但它始终是必须考虑的因素。


![图18.1 存储层次](https://raw.githubusercontent.com/ahuibo/Real-Time-Rendering-4th-CN/main/Real-Time_Rendering_4th_%E4%B8%AD%E6%96%87/assets/fig_18_4_18.1.png)

**图 18.1** 存储层次。沿金字塔向下，速度与成本都会降低。

性能分析器很难直接发现不良的内存访问模式。良好的模式必须从一开始就纳入设计 [1060]。下面列出编程时应考虑的一些要点。

- 代码中顺序访问的数据，在内存中也应顺序存放。例如，渲染三角网格时，如果访问顺序是纹理坐标 #0、法线 #0、颜色 #0、顶点 #0、纹理坐标 #1、法线 #1，就应按这个顺序连续存放。对于 GPU，这一点也可能很重要，例如变换后的顶点缓存（第 16.4.4 节）。关于为何把数据存成独立数据流有时更有利，另见第 16.4.5 节。
- 在关键代码中避免指针间接访问、跳转和函数调用，因为它们可能明显降低 CPU 性能。沿着一个指针找到另一个指针，再继续追踪，就会产生指针间接访问。现代 CPU 会尝试推测执行指令（分支预测）并预取内存（缓存预取），使所有功能单元都忙于执行代码。当循环中的代码流保持一致时，这些技术非常有效；面对二叉树、链表和图等分支数据结构时，它们却会失效，因此尽可能改用数组。McVoy 和 Staelin [1194] 给出了通过指针遍历链表的代码示例。这会使前后数据都发生缓存未命中，示例中 CPU 停顿时间超过了沿指针访问本身所需时间的 100 倍——后者是假设缓存能够提供指针地址时的耗时。Smits [1668] 指出，把基于指针的树展平成带跳跃指针的列表，可以显著改进层次遍历。使用 van Emde Boas 布局也是避免缓存未命中的方法，见第 19.1.4 节。高分支度的树往往优于二叉树，因为它们降低树的深度，从而减少间接访问次数。
- 将频繁使用的数据结构对齐到缓存行大小的整数倍，可以显著提高整体性能。例如，Intel 和 AMD 处理器上常见 64 字节缓存行 [1206]。编译器选项会有所帮助，但设计数据结构时就考虑对齐所需的填充（padding）更明智。Windows 和 Linux 上的 VTune、CodeAnalyst，Mac 上的 Instruments，以及 Linux 上的开源 Valgrind 等工具，都能帮助查找缓存瓶颈。对齐也会影响 GPU 着色器性能 [331]。
- 尝试不同的数据结构组织方式。例如，Hecker [698] 对一个简单的矩阵乘法程序试验了多种矩阵结构，节省的时间之多令人意外。对于特定架构，结构体数组：

```cpp
struct Vertex { float x, y, z; };
Vertex myvertices[1000];
```

或者数组结构体：

```cpp
struct VertexChunk { float x[1000], y[1000], z[1000]; };
VertexChunk myvertices;
```

可能表现更好。第二种结构更适合使用 SIMD 指令，但顶点数增多时，缓存未命中的可能性也会上升。随着数组规模增大，混合方案：

```cpp
struct Vertex4 { float x[4], y[4], z[4]; };
Vertex4 myvertices[250];
```

可能是最佳选择。

- 对于大小相同的对象，通常最好在启动时分配一个大内存池，再使用自己的分配和释放例程管理池内内存 [113, 736]。Boost 等库提供了池式分配。连续的一组记录，比单独分配产生的记录更可能具有缓存连贯性。不过，对于 C#、Java 等带垃圾回收的语言，内存池实际上可能降低性能。

虽然这不直接属于内存访问模式问题，但仍值得避免在渲染循环中分配或释放内存。使用内存池，一次性分配临时工作空间，并让栈、数组等结构只增长不缩小，用变量或标志记录哪些元素应视为已删除。

**注1：** 不要把这一研究领域与“数据驱动设计”混淆。后者可以指许多不同的东西，从 AWK 编程语言到 A/B 测试都包括在内。

### 18.4.2 API 调用

本书一直依据硬件的一般发展趋势提出建议。例如，带索引的顶点缓冲区对象，通常是向加速器提供几何数据的最快方式（第 16.4.5 节）。本节讨论怎样最有效地调用图形 API 本身。多数图形 API 的架构相似，已经有一些成熟的方法可供高效使用。

理解对象缓冲区的分配和存储，是高效渲染的基础 [1679]。对于具有 CPU 和独立 GPU 的桌面系统，两者通常各有自己的内存。图形驱动程序一般控制对象的驻留位置，但可以向它提供提示，说明把对象存在哪里最好。常见的分类是静态缓冲区与动态缓冲区。如果缓冲区数据每帧都改变，则最好使用不需要在 GPU 上永久占用存储空间的动态缓冲区。游戏主机、配有低功耗集成 GPU 的笔记本电脑，以及移动设备，通常采用统一内存，即 GPU 与 CPU 共享同一物理内存。即使在这种配置下，在正确的池中分配资源仍然很重要。正确地将资源标记为仅供 CPU 或仅供 GPU 使用，仍可带来好处。一般而言，如果一个内存区域必须被两个芯片共同访问，当其中一个写入时，另一个就必须使自己的缓存失效，确保不会读到过期数据；这是一项昂贵的操作。

如果对象不发生形变，或者形变能够完全由着色器程序完成（例如蒙皮），那么把对象数据存放在 GPU 内存中会很有利。把它存为静态缓冲区，就能表明对象数据不变。这样，每次渲染一帧时就不必再通过总线传输它，从而避免流水线在这里出现瓶颈。GPU 内部的内存带宽通常远高于 CPU 与 GPU 之间的总线带宽。

#### 状态改变

调用 API 会产生几种相关开销。在应用端，调用越多，应用耗时越长，无论调用实际做什么。这一开销可能很小，也可能明显，空驱动程序可以帮助识别它。依赖 GPU 数值的查询函数，可能由于与 CPU 同步时的停顿而使帧率减半 [1167]。这里深入讨论一种常见图形操作的优化：准备好流水线以绘制网格。这可能涉及改变状态，例如设置着色器及其 uniform 变量、绑定纹理、改变混合状态或所用颜色缓冲区，等等。

应用提高性能的一种主要方法，是把渲染状态相似的对象分组，从而尽量减少状态改变。GPU 是极其复杂的状态机，甚至可能是计算机科学中最复杂的状态机，因此改变状态可能很昂贵。少量成本可能涉及 GPU，但大部分来自驱动程序在 CPU 上的执行。如果 GPU 与 API 能很好地对应，状态改变的成本往往可预测，尽管仍然不小。如果 GPU 的功耗限制很严格、芯片面积有限（例如某些移动设备），或者需要绕过硬件缺陷，那么驱动程序可能不得不进行复杂的补救，产生出乎意料的高开销。状态改变的成本主要位于 CPU 端的驱动程序中。

一个具体例子是 PowerVR 架构如何支持混合。在旧 API 中，混合通过固定功能式接口指定。PowerVR 的混合是可编程的，这意味着驱动程序必须把当前混合状态补入像素着色器 [699]。在这种情况下，更先进的设计与 API 对应得不好，因而给驱动程序带来明显的设置开销。本章各处都提醒读者，硬件架构及其运行软件会影响各种优化的重要性；对于状态改变开销，这一点尤其如此。甚至具体 GPU 型号和驱动程序版本都可能产生影响。阅读本节时，请想象每页都盖着醒目的红字：“你的实际效果可能不同。”

Everitt 和 McDonald [451] 指出，不同类型的状态改变在成本上差别很大，并大致列出了 NVIDIA OpenGL 驱动程序每秒可以执行其中几种操作的次数。以下是他们在 2014 年给出的顺序，从最昂贵到最便宜：

- 渲染目标（帧缓冲区对象），约 6 万次/秒。
- 着色器程序，约 30 万次/秒。
- 混合模式（ROP），例如用于透明效果的模式。
- 纹理绑定，约 150 万次/秒。
- 顶点格式。
- uniform 缓冲区对象（UBO）绑定。
- 顶点绑定。
- uniform 更新，约 1000 万次/秒。

其他资料也支持这一大致的成本顺序 [488, 511, 741]。还有一种更昂贵的改变，是在 GPU 渲染模式与计算着色器模式之间切换 [1971]。可以对待显示对象排序，先按着色器分组，再按所用纹理分组，依此沿成本顺序向下，从而避免状态改变。按状态排序有时称为批处理。

另一种策略是重组对象数据，让更多数据可以共享。减少纹理绑定改变的一种常见方法，是把多幅纹理图像放进一张大纹理中，或者更好地放进纹理数组。如果 API 支持，无绑定纹理也是避免状态改变的选择（第 6.2.5 节）。相比更新 uniform，更换着色器程序通常较贵，因此一类材质内部的变化，可能更适合通过带有 if 语句的单个着色器来表达。共享着色器也可能让你建立更大的批次 [1609]。不过，让着色器更复杂也可能降低 GPU 性能。通过测量判断什么有效，是唯一可靠的办法。

向图形 API 发出数量更少、作用更大的调用，还能进一步节省开销。例如，多个 uniform 往往可以成组定义和设置，因此绑定单个 uniform 缓冲区对象要高效得多 [944]。在 DirectX 中，它们称为常量缓冲区。正确使用它们，既节省每次函数调用的时间，也节省每个独立 API 调用内部进行错误检查的时间 [331, 613]。

现代驱动程序常常把状态设置延迟到遇见第一个绘制调用时。如果在此之前出现冗余 API 调用，驱动程序就会过滤掉它们，避免实际进行状态改变。通常用脏标志记录需要改变状态，因此每次绘制调用之后都恢复到基础状态，可能很昂贵。例如，准备绘制对象时，你可能希望默认认为状态 X 已关闭。一种实现方式是先执行 `Enable(X); Draw(M1); Disable(X);`，再执行 `Enable(X); Draw(M2); Disable(X);`，这样每次绘制后都会恢复状态。然而，这也很可能在两次绘制调用之间再次设置状态而浪费大量时间，尽管两者之间实际上并不需要改变状态。

应用通常掌握关于何时需要改变状态的更高层信息。例如，从不透明表面的 replace 混合模式切换到透明表面的 over 模式，通常每帧只需要做一次。完全可以避免在每个对象渲染前都设置混合模式。Galeano [511] 表明，如果不进行这种过滤，而发出不必要的状态调用，他们的 WebGL 应用每帧就会多花接近 2 ms。不过，如果驱动程序已经能够高效过滤冗余，那么应用对每个调用重复做同样的检查也可能是浪费。应在过滤 API 调用上投入多少工作，主要取决于底层驱动程序 [443, 488, 741]。

#### 合并与实例化

高效使用 API 可以避免让 CPU 成为瓶颈。API 的另一个问题是小批次问题。如果忽视它，它可能成为影响现代 API 性能的重要因素。简单说，渲染少数含有大量三角形的网格，比渲染许多小而简单的网格高效得多。这是因为每个绘制调用都有固定开销，无论图元大小如何，处理图元都要付出这项代价。

早在 2003 年，Wloka [1897] 就表明，每批绘制两个相对较小的三角形时，吞吐量距离被测 GPU 的最大吞吐量相差 375 倍。注2 在 2.7 GHz CPU 上，得到的不是每秒 1.5 亿个三角形，而是每秒 40 万个。若场景由大量小而简单的对象构成，每个仅包含几个三角形，那么性能会完全受 API 在 CPU 上的开销限制；GPU 无法进一步提高它。也就是说，CPU 处理绘制调用的时间，超过 GPU 实际绘制网格所需的时间，使 GPU 得不到足够工作。

Wloka 的经验法则是：“每帧你只能得到 X 个批次。”这是每帧能够发出的绘制调用数的上限，纯粹是由 CPU 成为限制因素造成的。2003 年，API 成为瓶颈的分界点约为每个对象 130 个三角形。图 18.2 显示，到 2006 年，这一分界点升到了每个网格 510 个三角形。时代已经变化。人们做了大量工作来缓解绘制调用问题，CPU 也变得更快。2003 年的建议是每帧 300 次绘制调用；2012 年，一个团队的上限是每帧 16000 次 [1381]。即便如此，对于某些复杂场景，这个数目仍然不够。采用 DirectX 12、Vulkan、Metal 等现代 API，可以把驱动程序本身的开销降得很低，这是它们的一项主要优势 [946]。不过，GPU 对每个网格也可能有自己的固定成本。

**注2：** Wloka 使用 batch 一词表示通过一次绘制调用渲染的单个网格。多年来，这个术语的含义有所扩展，现在有时也表示一组具有相同状态、将要渲染的独立对象，因为这样能降低 API 开销。


![图18.2 批次性能基准](https://raw.githubusercontent.com/ahuibo/Real-Time-Rendering-4th-CN/main/Real-Time_Rendering_4th_%E4%B8%AD%E6%96%87/assets/fig_18_4_18.2.png)

**图 18.2** 在 Intel Core 2 Duo 2.66 GHz CPU、NVIDIA G80 GPU 上运行 DirectX 10 的批次性能基准测试。测试在不同条件下运行不同大小的批次并计时。“Low”条件使用只有位置数据的三角形和恒定颜色像素着色器；另一组测试使用正常规模的网格与着色。“Single”表示多次渲染一个批次。“Instancing”复用网格数据，并把逐实例数据放在独立数据流中。“Constants”是 DirectX 10 的一种方法，把实例数据放入常量内存。可以看到，小批次会损害所有方法的性能，但实例化在比例上提供了快得多的性能。到几百个三角形时，性能趋于平稳，因为瓶颈变为从顶点缓冲区和缓存取出顶点的速度。（图表由 NVIDIA Corporation 提供。）

减少绘制调用数量的一种方法，是把多个对象合并成一个网格，只需一次绘制调用就能渲染整个集合。对于使用相同状态、且至少相互之间保持静止的一组对象，合并只需做一次，之后每帧都可复用这个批次 [741, 1322]。能够合并网格，也是考虑通过共用着色器和纹理共享技术来避免状态改变的另一个理由。合并带来的节省，不仅来自减少 API 绘制调用，也来自应用本身要处理的对象变少。然而，批次如果远大于所需规模，可能使视锥体剔除等其他算法效率降低 [1381]。一种做法是借助包围体层次结构，查找并分组彼此靠近的静态对象。合并的另一个问题是选择操作，因为所有静态对象在同一个网格中不再有区分。典型解决方案是在网格的每个顶点处保存对象标识符。

尽量减少应用处理与 API 开销的另一种方法，是采用某种实例化形式 [232, 741, 1382]。多数 API 支持通过一次调用将一个对象绘制多次。通常，先指定一个基础模型，再提供独立数据结构，保存每个所需实例的信息。除位置与朝向外，还可以逐实例指定其他属性，例如树叶颜色、受风产生的弯曲程度，或任何可供着色器程序影响模型的参数。大量使用实例化，就能创建郁郁葱葱的丛林场景，见图 18.3。人群场景也非常适合实例化：从一组备选项中选择不同身体部件，可以让每个角色看起来不同。随机着色和贴花还能进一步增加变化。实例化还可以与细节层次技术结合 [122, 1107, 1108]，图 18.4 展示了一个例子。


![图18.3 植被实例化](https://raw.githubusercontent.com/ahuibo/Real-Time-Rendering-4th-CN/main/Real-Time_Rendering_4th_%E4%B8%AD%E6%96%87/assets/fig_18_4_18.3.png)

**图 18.3** 植被实例化。下图中所有颜色相同的对象，都在一次绘制调用中渲染 [1869]。（图像来自 CryEngine1，由 Crytek 提供。）


![图18.4 人群场景](https://raw.githubusercontent.com/ahuibo/Real-Time-Rendering-4th-CN/main/Real-Time_Rendering_4th_%E4%B8%AD%E6%96%87/assets/fig_18_4_18.4.png)

**图 18.4** 人群场景。使用实例化可以尽量减少所需的绘制调用数量。这里还采用了细节层次技术，例如为远处模型渲染替身图像 [1107, 1108]。（图像由 Jonathan Maïm、Barbara Yersin、Mireille Clavien 和 Daniel Thalmann 提供。）

一种结合合并与实例化的概念称为合并实例化（merge-instancing）：合并后的网格中包含的对象，本身又可以被实例化 [146, 1382]。理论上，几何着色器可以用于实例化，因为它能复制输入网格的数据。实际中，如果需要很多实例，这种方法可能比使用 API 实例化命令更慢。几何着色器的设计意图是对数据进行局部、小规模的扩增 [1827]。此外，在某些架构中，例如 Mali 的分块渲染器，几何着色器由软件实现。引用 Mali 最佳实践指南 [69] 的话：“为你的问题寻找更好的解决办法。几何着色器不是你的解决方案。”

### 18.4.3 几何阶段

几何阶段负责变换、逐顶点光照、裁剪、投影和屏幕映射。本书其他章节讨论了减少流水线中数据量的方法。高效的三角网格存储、模型简化、顶点数据压缩（第 16 章）都能同时节省处理时间和内存。视锥体剔除、遮挡剔除等技术（第 19 章）可以避免把整个图元送入流水线。在 CPU 上加入这些大尺度技术，可以彻底改变应用的性能特征，因此值得在开发早期就尝试。在 GPU 上，这类技术不那么常见。一个值得注意的例子是，可以利用计算着色器执行各种剔除 [1883, 1884]。

光照各项的效果可以逐顶点计算，也可以逐像素计算（在像素处理阶段），或者二者结合。光照计算有多种优化方式。首先应考虑所使用的光源类型。所有三角形都需要光照吗？有时，一个模型只需要纹理、纹理加顶点颜色，或者仅需要顶点颜色。

如果光源相对于几何体保持静止，就可以预计算漫反射光照与环境光照，并将它们作为顶点颜色保存。这通常称为把光照“烘焙”进去。更复杂的预光照形式，是预计算场景中的漫反射全局光照（第 11.5.1 节）。这种光照可以作为顶点颜色、顶点强度或光照贴图保存。

对于前向渲染系统，光源数量会影响几何阶段的性能。光源越多，计算就越多。减少工作量的一种常见方法，是禁用或缩减局部光照，改用环境贴图（第 10.5 节）。

### 18.4.4 光栅化阶段

光栅化可以从几个方面优化。对于封闭的实体对象，以及永远不会露出背面的对象（例如房间内墙壁的背面），应开启背面剔除（第 19.3 节）。这会使需要光栅化的三角形数量大约减半，从而减轻三角形遍历的负担。此外，如果像素着色计算很昂贵，这样做尤其有益，因为背面完全不会被着色。

### 18.4.5 像素处理阶段

优化像素处理往往很有收益，因为通常需要着色的像素远多于顶点。但也存在值得注意的例外。即使一次绘制最终没有产生任何可见像素，顶点也总要处理。如果渲染引擎的剔除不够有效，顶点着色的成本可能超过像素着色。太小的三角形不仅可能造成超出所需的顶点着色计算，还会产生更多仅部分覆盖的四像素块，带来额外工作。更重要的是，只覆盖少量像素的带纹理网格，线程占用率往往很低。如第 3.10 节所述，纹理采样耗时很大，GPU 通过切换去执行其他片元的着色器程序来隐藏这段时间，等到纹理数据取回后再返回。低占用率会使延迟隐藏效果变差。使用大量寄存器的复杂着色器，也会因为同一时刻可用线程更少而降低占用率（第 23.3 节）。这种情况称为高寄存器压力。还存在其他细微因素，例如频繁切换到其他 warp，可能导致更多缓存未命中。Wronski [1911, 1914] 讨论了各种占用率问题和解决办法。

首先，应使用原生纹理与像素格式，也就是图形加速器内部使用的格式，以避免可能代价高昂的格式转换 [278]。另外两种纹理相关技术，是只加载需要的 mipmap 层级（第 19.10.1 节），以及采用纹理压缩（第 6.2.6 节）。一如既往，纹理越小、越少，内存占用越低，进而传输与访问耗时越少。纹理压缩也能提高缓存性能，因为同样大小的缓存可以容纳更多像素。

一种细节层次技术，是根据对象与观察者的距离，采用不同的像素着色器程序。例如，场景中有三个飞碟模型，最近的飞碟可以使用精细的凹凸贴图表现表面细节，而较远的两个则不需要。最远的飞碟还可以简化或完全去掉镜面高光，既简化计算，也减少“萤火虫”，即欠采样造成的闪烁伪影。在简化模型上使用逐顶点颜色，还有一个额外好处：不会因纹理变化而需要改变状态。

只有当三角形光栅化时片元可见，才会调用像素着色器。GPU 的 early-z 测试（第 23.7 节）将片元的 z 深度与 z 缓冲区比较。如果不可见，片元直接被丢弃，不执行任何像素着色器计算，从而节省大量时间。像素着色器虽然可以修改 z 深度，但这样就无法进行 early-z 测试。

为了理解程序行为，尤其是像素处理阶段的负载，将深度复杂度可视化很有帮助。深度复杂度就是覆盖一个像素的表面数量。图 18.5 给出了例子。生成深度复杂度图像的一种简单方法，是禁用 z 缓冲，并使用类似 OpenGL 的 `glBlendFunc(GL_ONE, GL_ONE)` 调用。首先将图像清为黑色，再以颜色 (1/255, 1/255, 1/255) 渲染场景中的全部对象。这一混合函数设置的作用是：每渲染一个图元，被写入的像素值就增加一个强度等级。因此，深度复杂度为 0 的像素是黑色，深度复杂度为 255 的像素是纯白色，即 (255, 255, 255)。


![图18.5 深度复杂度](https://raw.githubusercontent.com/ahuibo/Real-Time-Rendering-4th-CN/main/Real-Time_Rendering_4th_%E4%B8%AD%E6%96%87/assets/fig_18_4_18.5.png)

**图 18.5** 右图显示左侧场景的深度复杂度。（图像使用 NVIDIA Corporation 的 NVPerfHUD 创建。）

像素的过度绘制量，与实际渲染了多少个表面有关。再次渲染场景，但启用 z 缓冲，就能得到像素着色器执行的次数。过度绘制，是指花费工作计算了某个表面的着色结果，却又被后续像素着色器调用产生的表面遮住。延迟渲染（第 20.1 节）的一个优点——光线追踪也是如此——就是先完成所有可见性计算，再进行着色。

假设两个三角形覆盖同一个像素，深度复杂度就是 2。如果先绘制较远的三角形，较近的三角形就会覆盖它，过度绘制量为 1。如果先绘制较近的三角形，较远的三角形无法通过深度测试，不会被绘制，因此没有过度绘制。对于以随机顺序覆盖一个像素的一组不透明三角形，平均绘制次数由调和级数给出 [296]：


![数学公式](https://raw.githubusercontent.com/ahuibo/Real-Time-Rendering-4th-CN/main/Real-Time_Rendering_4th_%E4%B8%AD%E6%96%87/assets/math/eq_18_04_cfc290568a3ca3.png)


其中的逻辑是：第一个被渲染的三角形产生一次绘制。第二个三角形要么在第一个前面，要么在后面，两种可能各占 50%。第三个三角形相对于前两个可以有三种位置，因此有三分之一的机会处在最前面。当 n 趋于无穷时，


![数学公式](https://raw.githubusercontent.com/ahuibo/Real-Time-Rendering-4th-CN/main/Real-Time_Rendering_4th_%E4%B8%AD%E6%96%87/assets/math/eq_18_04_89bce58e59682c.png)


其中 γ = 0.57721… 是欧拉—马歇罗尼常数。在深度复杂度较低时，过度绘制增加得很快，但增长很快就趋缓。例如，深度复杂度为 4 时平均绘制 2.08 次，为 11 时平均绘制 3.02 次；但需要达到 12367 的深度复杂度，平均绘制次数才达到 10.00。

> 译注：公式（18.2）忠实保留原书排印。其意图是调和数的渐近关系；严格表述应为 H(n) − ln(n) 在 n → ∞ 时趋于 γ，或写作 H(n) = ln(n) + γ + o(1)。原式在取极限后的右端仍含 n，表述不严谨。

因此，过度绘制不一定像看起来那么糟，但我们仍希望尽量减少它，又不花费太多 CPU 时间。将场景中的不透明对象粗略排序，再按大致从前到后（由近及远）的顺序绘制，是减少过度绘制的常用方法 [240, 443, 488, 511]。之后绘制的被遮挡对象不会写入颜色缓冲区或 z 缓冲区，也就是减少了过度绘制。此外，像素片元甚至在到达像素着色器程序之前，就可以被遮挡剔除硬件拒绝（第 23.5 节）。有许多方法可以实现排序。一种简单方法是，根据所有不透明对象的质心沿观察方向的距离进行显式排序。如果已经用包围体层次结构或其他空间结构进行视锥体剔除，就可以优先遍历较近的子节点，并沿着层次结构继续这样处理。

对于像素着色器程序复杂的表面，另一种技术可能很有用。执行 z 预通道时，先只将几何体渲染到 z 缓冲区，然后正常渲染整个场景 [643]。这可以消除过度绘制造成的所有着色器计算，但代价是必须对全部几何体额外完整处理一次。Pettineo [1405] 写道，他们团队在电子游戏中使用深度预通道，主要就是为了避免过度绘制。不过，按大致从前到后的顺序绘制，可能不需要这项额外工作就能获得大部分相同收益。一种混合方法，是识别并优先绘制少量大型、简单、最可能带来收益的遮挡体 [1768]。正如 McGuire [1177] 所说，对于他的特定系统，完整绘制的预通道没有改善性能。只有测量，才能知道哪项技术对你的应用最有效，或者这些技术是否有效。

前面建议按着色器和纹理分组，尽量减少状态改变；这里则讨论按距离排序后渲染对象。这两个目标通常产生不同的对象绘制顺序，因此彼此冲突。对于给定场景和视点，总有某个理想绘制顺序，但很难预先找到。也可以采用混合方案，例如将附近对象按深度排序，其余对象按材质排序 [1433]。一种常见而灵活的解决办法 [438, 488, 511, 1434, 1882]，是为每个对象创建排序键，给每项相关标准分配一组位，将它们全部封装进去，见图 18.6。


![图18.6 绘制顺序排序键](https://raw.githubusercontent.com/ahuibo/Real-Time-Rendering-4th-CN/main/Real-Time_Rendering_4th_%E4%B8%AD%E6%96%87/assets/fig_18_4_18.6.png)

**图 18.6** 绘制顺序排序键的例子。键按从小到大排序。设置透明位意味着对象透明，因为透明对象要在所有不透明对象之后渲染。对象到摄像机的距离以低精度整数存储。对于透明对象，距离值需要反转或取负，因为我们希望它们按从后到前的顺序排列。每个着色器都有唯一标识号，纹理也是如此。

我们可以优先按距离排序，但通过限制保存深度的位数，使处于某一距离范围内的对象仍能按着色器分组。仅把绘制划入两三个深度分区也很常见。如果一些对象深度相同，使用的着色器也相同，就用纹理标识符排序，从而把使用相同纹理的对象分到一起。

这是一个简单且依赖具体情境的例子。例如，渲染引擎本身可能已经把不透明对象与透明对象分开，因此不需要透明位。其他字段的位数当然也会随预期的着色器、纹理最大数量而变化。可以加入或替换其他字段，例如混合状态字段、z 缓冲区读写字段。最重要的还是架构。例如，移动设备上某些基于分块的 GPU 渲染器，无法从前到后排序中获得任何好处，因此状态排序才是唯一重要的优化项 [1609]。这里的核心思想是：把所有属性放入单个整数键中，就能高效排序，尽可能减少过度绘制和状态改变。

### 18.4.6 帧缓冲区技术

渲染一个场景，常常涉及大量帧缓冲区访问和像素着色器执行。为了减轻缓存层次的压力，一条常见建议是缩小帧缓冲区中每个像素的存储大小。虽然每个颜色通道使用 16 位浮点值可以提供更高精度，但 8 位值只有其一半大小；只要精度足够，访问就会更快。JPEG、MPEG 等许多图像和视频压缩方案，常常对色度进行子采样。由于人类视觉系统对亮度比对色度更加敏感，这样做对视觉的影响往往可以忽略。例如，Frostbite 游戏引擎 [1877] 使用这种色度子采样思想，降低对每通道 16 位图像进行后处理时的带宽成本。

Mavridis 和 Papaioannou [1144] 提议，使用书页 197 所述的有损 YCoCg 变换，在光栅化过程中对颜色缓冲区取得类似效果。他们的像素布局见图 18.7。与 RGBA 相比，这让颜色缓冲区的存储需求减半（假设不需要 A），并且通常能够提高性能，具体取决于架构。由于每个像素只有一个色度分量，需要用重建滤波器推导每个像素的完整 YCoCg，再在显示前转换回 RGB。例如，对于缺少 ![数学符号](https://raw.githubusercontent.com/ahuibo/Real-Time-Rendering-4th-CN/main/Real-Time_Rendering_4th_%E4%B8%AD%E6%96%87/assets/math/eq_18_04_289cf5353106a3.png) 值的像素，可以取距离最近的四个 ![数学符号](https://raw.githubusercontent.com/ahuibo/Real-Time-Rendering-4th-CN/main/Real-Time_Rendering_4th_%E4%B8%AD%E6%96%87/assets/math/eq_18_04_289cf5353106a3.png) 值的平均值。不过，这样重建边缘的效果不够理想。因此，改用一个简单的边缘感知滤波器，其实现为：


![图18.7 颜色缓冲区的棋盘布局](https://raw.githubusercontent.com/ahuibo/Real-Time-Rendering-4th-CN/main/Real-Time_Rendering_4th_%E4%B8%AD%E6%96%87/assets/fig_18_4_18.7.png)

**图 18.7** 左：4 × 2 个像素，每个保存四个颜色分量（RGBA）。右：另一种表示方式，每个像素保存亮度 Y，以及第一色度分量 ![数学符号](https://raw.githubusercontent.com/ahuibo/Real-Time-Rendering-4th-CN/main/Real-Time_Rendering_4th_%E4%B8%AD%E6%96%87/assets/math/eq_18_04_289cf5353106a3.png) 或第二色度分量 ![数学符号](https://raw.githubusercontent.com/ahuibo/Real-Time-Rendering-4th-CN/main/Real-Time_Rendering_4th_%E4%B8%AD%E6%96%87/assets/math/eq_18_04_582ab39000dac7.png)，二者按棋盘格交替排列。


![数学公式](https://raw.githubusercontent.com/ahuibo/Real-Time-Rendering-4th-CN/main/Real-Time_Rendering_4th_%E4%B8%AD%E6%96%87/assets/math/eq_18_04_e05845a3f76c89.png)


该公式用于没有 ![数学符号](https://raw.githubusercontent.com/ahuibo/Real-Time-Rendering-4th-CN/main/Real-Time_Rendering_4th_%E4%B8%AD%E6%96%87/assets/math/eq_18_04_289cf5353106a3.png) 的像素。其中 ![数学符号](https://raw.githubusercontent.com/ahuibo/Real-Time-Rendering-4th-CN/main/Real-Time_Rendering_4th_%E4%B8%AD%E6%96%87/assets/math/eq_18_04_a576c2b5e63572.png) 和 ![数学符号](https://raw.githubusercontent.com/ahuibo/Real-Time-Rendering-4th-CN/main/Real-Time_Rendering_4th_%E4%B8%AD%E6%96%87/assets/math/eq_18_04_b456bbb8033d74.png) 是当前像素左、右、上、下相邻像素的值；L 是当前像素的亮度；t 是边缘检测阈值。Mavridis 和 Papaioannou 使用 t = 30/255。当 x < 0 时，step(x) 函数为 0，否则为 1。因此，滤波权重 ![数学符号](https://raw.githubusercontent.com/ahuibo/Real-Time-Rendering-4th-CN/main/Real-Time_Rendering_4th_%E4%B8%AD%E6%96%87/assets/math/eq_18_04_3e929edc96716e.png) 为 0 或 1；当亮度梯度 |![数学符号](https://raw.githubusercontent.com/ahuibo/Real-Time-Rendering-4th-CN/main/Real-Time_Rendering_4th_%E4%B8%AD%E6%96%87/assets/math/eq_18_04_b456bbb8033d74.png) − L| 大于 t 时，权重为 0。网上可以找到附源代码的 WebGL 演示 [1144]。

> 译注：公式（18.3）及上述解释均照原书保留，原文内部存在不一致。按照本页给出的 step 定义，当 |![数学符号](https://raw.githubusercontent.com/ahuibo/Real-Time-Rendering-4th-CN/main/Real-Time_Rendering_4th_%E4%B8%AD%E6%96%87/assets/math/eq_18_04_b456bbb8033d74.png) − L| > t 时，排印公式得到的权重为 1，而不是文字所述的 0；公式也未显示加权平均通常需要的权重归一化分母。这里不自行改动原式。

由于显示分辨率持续提高，同时希望节省着色器执行成本，已有多个系统采用棋盘格模式进行渲染 [231, 415, 836, 1885]。在虚拟现实应用中，Vlachos [1824] 对视野周边的像素使用棋盘格模式；Answer [59] 则使每个 2 × 2 四像素块减少一至三个采样。

### 18.4.7 合并阶段

确保只在有用时才启用混合模式。理论上，可以为每个三角形都设置 over 合成，无论它透明还是不透明，因为使用 over 的不透明表面会完全覆盖像素中的值。但这比简单的 replace 光栅操作更昂贵，因此值得跟踪使用镂空纹理的对象和带透明度的材质。另一方面，有些光栅操作不会增加成本。例如，使用 z 缓冲区时，在某些系统上同时访问模板缓冲区并不会增加耗时，因为 8 位模板缓冲区值和 24 位 z 深度值保存在同一个字中 [890]。

值得仔细考虑，各种缓冲区应在何时使用或清除。由于 GPU 具有快速清除机制（第 23.5 节），建议始终同时清除颜色和深度缓冲区，因为这能提高这些缓冲区的内存传输效率。

通常应尽可能避免从 GPU 向 CPU 回读渲染目标。CPU 对帧缓冲区的任何访问，都会在返回渲染结果前迫使整个 GPU 流水线排空，使其中的全部并行性丧失 [1167, 1609]。

如果发现合并阶段确实是瓶颈，可能需要重新考虑你的方法。是否可以使用较低精度的输出目标，或许借助压缩？有没有办法重排算法，减轻这一阶段的压力？对于阴影，能否缓存并复用那些没有任何物体移动的部分？

本节讨论了如何通过寻找瓶颈和调整性能，充分利用每个阶段。不过，要警惕反复优化同一个算法的风险：有时采用一种完全不同的技术，可能更有帮助。


## 18.5 多处理

来源：原书书页 805—814（PDF 第 826—835 页），从“18.5 Multiprocessing”开始，至“Further Reading and Resources”之前。全部下级小节及图 18.8—18.12 均收录于此；章末延伸阅读另见《18.99_延伸阅读与资源》。

传统 API 的发展方向，是减少调用次数，让每次调用完成更多工作 [443, 451]。新一代 API——DirectX 12、Vulkan 和 Metal——采用了不同的策略。对于这些 API，驱动程序经过精简，规模尽可能小；状态验证的大部分复杂工作与责任，以及内存分配等其他功能，都转移给了调用它们的应用程序 [249, 1438, 1826]。这种重新设计，在很大程度上是为了尽量减少绘制调用和状态变更的开销；这些开销来自将旧式 API 映射到现代 GPU 的过程。这些新 API 鼓励的另一件事，是使用多个 CPU 处理器来调用 API。

在 2003 年前后，由于散热和功耗等若干物理问题，CPU 时钟频率不断上升的趋势在约 3.4 GHz 处趋于平缓 [1725]。这些限制促成了支持多处理的 CPU：不再提高时钟频率，而是在单颗芯片中放入更多 CPU。事实上，大量小型核心能够提供最佳的单位面积性能 [75]，这也是 GPU 本身如此高效的主要原因。此后，如何编写能够利用并发的高效、可靠程序，一直是一项挑战。本节将介绍在 CPU 核心上高效进行多处理的基本概念，最后讨论图形 API 如何演进，以便让驱动程序内部支持更多并发。

多处理器计算机大体可以分为消息传递架构和共享内存多处理器。在消息传递设计中，每个处理器都有自己的内存区域，处理器之间通过发送消息来交流结果。这类设计在实时渲染中并不常见。共享内存多处理器正如其名：所有处理器共同使用一个逻辑内存地址空间。最常见的多处理器系统使用共享内存，而其中大多数采用对称多处理（symmetric multiprocessing，SMP）设计。SMP 意味着所有处理器都是相同的。多核 PC 系统就是对称多处理架构的一个例子。

这里，我们将介绍两种在实时图形中使用多个处理器的一般方法。第一种是多处理器流水线，也称时间并行；对它的介绍会比第二种更详细。第二种是并行处理，也称空间并行。图 18.8 展示了这两种方法。随后，基于任务的多处理会将这两类并行结合起来：应用程序创建作业，每个作业都可以由一个独立核心领取并处理。

### 18.5.1 多处理器流水线

如前所述，流水线是一种加快执行速度的方法：把一项作业划分成若干可并行执行的流水线阶段。一个流水线阶段的结果会传递到下一个阶段。对于 n 个流水线阶段，理想加速比是 n 倍，而最慢的阶段（瓶颈）决定实际加速比。到目前为止，我们所看到的流水线，都是利用一个 CPU 核心和一个 GPU，使应用程序、几何处理、光栅化和像素处理并行运行。当主机有多个处理器可用时，也可以使用流水线；在这些情况下，它称为多进程流水线或软件流水线。


![图18.8：多处理器流水线与并行处理](https://raw.githubusercontent.com/ahuibo/Real-Time-Rendering-4th-CN/main/Real-Time_Rendering_4th_%E4%B8%AD%E6%96%87/assets/fig_18_5_18.8.png)

**图 18.8。** 使用多个处理器的两种不同方式。上图展示三个处理器（CPU）如何用于多处理器流水线，下图展示在三个 CPU 上并行执行的情况。这两种实现的一个区别是，采用下方的配置可以获得更低的延迟。另一方面，多处理器流水线可能更容易使用。两种配置的理想加速比都是线性的，也就是说，使用 n 个 CPU 可以获得 n 倍的加速。

这里介绍一种软件流水线。它可以有无穷多种变体，具体方法应适应特定应用。在这个例子中，应用程序阶段被划分为三个阶段 [1508]：APP、CULL 和 DRAW。这是粗粒度流水线，意味着每个阶段都相对较长。APP 阶段是流水线的第一个阶段，因此由它控制其他阶段。应用程序开发者可以在这个阶段加入额外代码，例如进行碰撞检测。这个阶段还会更新视点。CULL 阶段可以执行：

- 对场景图进行遍历和层次化视锥剔除（第 19.4 节）。
- 选择细节层次（第 19.9 节）。
- 状态排序，如第 18.4.5 节所述。
- 最后，生成一份包含所有应渲染对象的简单列表；这一步始终执行。

DRAW 阶段接收 CULL 阶段生成的列表，发出列表中的所有图形调用。这意味着，它只需遍历列表，向 GPU 提交工作。图 18.9 展示了使用这条流水线的几种例子。

如果有一个处理器核心可用，那么三个阶段都在这个核心上运行。如果有两个 CPU 核心可用，那么 APP 和 CULL 可以在一个核心上执行，DRAW 则在另一个核心上执行。另一种配置是在一个核心上执行 APP，在另一个核心上执行 CULL 和 DRAW。哪种配置最好，取决于各阶段的工作负载。最后，如果主机有三个核心可用，那么每个阶段都可以在一个独立核心上执行。图 18.10 展示了这种可能性。


![图18.9：多处理器流水线的不同配置](https://raw.githubusercontent.com/ahuibo/Real-Time-Rendering-4th-CN/main/Real-Time_Rendering_4th_%E4%B8%AD%E6%96%87/assets/fig_18_5_18.9.png)

**图 18.9。** 多处理器流水线的不同配置。粗线表示阶段之间的同步，下标表示帧编号。最上方展示单 CPU 流水线。中间和下方展示使用两个 CPU 时两种不同的流水线划分方式。中间的配置让 APP 和 CULL 占用一个流水线阶段，DRAW 占用另一个流水线阶段。如果 DRAW 的工作量远大于其他阶段，这样划分就很合适。下方的配置让 APP 占用一个流水线阶段，其余两个占用另一个阶段。如果 APP 的工作量远大于其他阶段，这种划分就很合适。注意，下面两种配置为 APP、CULL 和 DRAW 阶段提供了更多时间。

这种技术的优点是提高了吞吐量，也就是渲染速度。缺点是，与并行处理相比，其延迟更大。延迟，或称时间延迟，是指从轮询用户动作到产生最终图像所经历的时间 [1849]。不要将它与帧率混淆，帧率是每秒显示的帧数。例如，假设用户正在使用一台无线头戴式显示器。确定头部位置的信息可能需要 10 毫秒才能到达 CPU，随后渲染一帧需要 15 毫秒。那么，从最初输入到显示的延迟就是 25 毫秒。即使帧率为 66.7 Hz（1 ÷ 0.015 秒），如果不进行位置预测或其他补偿，交互仍可能让人感觉迟钝，因为位置变化传送至 CPU 存在延迟。忽略用户交互带来的延迟（这在两种系统中是一个相同的常量）后，多处理器流水线比并行处理的延迟更大，因为前者使用流水线。下一小节将详细讨论：并行处理会将一帧的工作拆分成若干部分，并发执行。


![图18.10：三阶段流水线与重叠执行](https://raw.githubusercontent.com/ahuibo/Real-Time-Rendering-4th-CN/main/Real-Time_Rendering_4th_%E4%B8%AD%E6%96%87/assets/fig_18_5_18.10.png)

**图 18.10。** 上图展示一条三阶段流水线。与图 18.9 中的配置相比，这种配置为每个流水线阶段提供了更多时间。下图展示一种降低延迟的方法：让 CULL 与 DRAW 重叠执行，并在两者之间使用先进先出（FIFO）缓冲。

与主机仅使用一个 CPU 相比，多处理器流水线能够获得更高的帧率，而延迟基本相同，或因同步成本而略有增加。延迟会随流水线阶段数的增加而增加。对于负载均衡良好的应用程序，使用 n 个 CPU 可以获得 n 倍的加速。

一种降低延迟的技术，是在 APP 阶段结束时更新视点，以及其他对延迟敏感的参数 [1508]。这样可以将延迟减少大约一帧。另一种降低延迟的方法，是让 CULL 和 DRAW 重叠执行。这意味着，只要有任何内容准备好可以渲染，CULL 的结果就立刻传给 DRAW。要实现这一点，两个阶段之间必须有某种缓冲，通常采用 FIFO。缓冲为空或已满时，相应阶段会停顿：当缓冲已满时，CULL 必须停顿；当缓冲为空时，DRAW 则因无工作可做而等待。缺点是，状态排序等技术无法再得到同等程度的运用，因为图元一经 CULL 处理完毕就必须渲染。图 18.10 展示了这种降低延迟的技术。

图中的流水线最多使用三个 CPU，各阶段承担特定任务。不过，这项技术绝不限于这种配置：你可以使用任意数量的 CPU，并按任意方式划分工作。关键在于合理划分需要完成的整项作业，使流水线趋于均衡。多处理器流水线技术所需的同步很少，因为它只需要在切换帧时同步。额外的处理器也可以用于并行处理，但那需要更频繁的同步。

### 18.5.2 并行处理

使用多处理器流水线技术的一个主要缺点，是延迟往往会增加。对于飞行模拟器、第一人称射击游戏以及虚拟现实渲染等应用，这是不可接受的。移动视点时，通常希望立即得到响应，也就是在下一帧得到响应；但延迟较长时就做不到。不过，这也取决于具体情况。如果多处理使帧率从延迟为 1 帧的 30 FPS，提高到延迟为 2 帧的 60 FPS，那么多出来的这一帧延迟并不会造成可察觉的差异。

如果有多个处理器可用，也可以尝试让若干段代码并发运行，这可能缩短延迟。为此，程序的任务必须具备可并行性。将算法并行化有几种不同方法。假设有 n 个处理器可用。采用静态分配 [313] 时，把整个工作包，例如遍历一个加速结构，划分为 n 个工作包。随后每个处理器负责一个工作包，所有处理器并行执行各自的工作包。所有处理器都完成工作包后，可能需要合并各处理器的结果。要让这种方法奏效，工作负载必须具有很高的可预测性。

如果不是这种情况，就可以使用能够适应不同工作负载的动态分配算法 [313]。这些算法使用一个或多个工作池。作业生成后就放入工作池。CPU 完成当前作业后，可以从队列中领取一个或多个作业。必须谨慎处理，确保某项作业只能被一个 CPU 领取，同时确保维护队列的开销不会损害性能。作业较大时，维护队列的开销就不那么成问题；但另一方面，如果作业过大，系统失衡也可能造成性能下降，也就是说，一个或多个 CPU 可能因没有工作可做而等待。

与多处理器流水线一样，一个在 n 个处理器上运行的并行程序，理想加速比是 n 倍。这称为线性加速。虽然很少真正达到线性加速，但实际结果有时也能接近它。

书页 807 的图 18.8 同时展示了一条多处理器流水线和一个并行处理系统，两者都使用三个 CPU。暂且假设，它们每帧需要完成的工作量相同，而且两种配置都能达到线性加速。这意味着，与串行执行（也就是在单个 CPU 上执行）相比，其执行速度将提高到三倍。此外，我们假设每帧的全部工作需要 30 ms，这意味着单个 CPU 上的最高帧率约为 1 ÷ 0.03 ≈ 33 帧/秒。

理想情况下，多处理器流水线会将工作分为三个大小相等的工作包，让每个 CPU 负责其中一个工作包。每个工作包应当需要 10 ms 完成。如果沿着流水线跟踪工作流，就会看到流水线中的第一个 CPU 工作 10 ms（即完成作业的三分之一），然后将工作传给下一个 CPU。第一个 CPU 随即开始处理下一帧的第一部分。当一帧最终完成时，完成这一帧总共花费了 30 ms；但由于工作在流水线中并行进行，因此每 10 ms 就有一帧完成。所以，延迟为 30 ms，加速比为三倍（30 ÷ 10），最终达到每秒 100 帧。

同一程序的并行版本也会把作业分为三个工作包，但这三个工作包会在三个 CPU 上同时执行。这意味着延迟为 10 ms，而一帧的工作同样需要 10 ms。由此可见，与多处理器流水线相比，并行处理的延迟要短得多。


![图18.11：Frostbite的CPU作业图](https://raw.githubusercontent.com/ahuibo/Real-Time-Rendering-4th-CN/main/Real-Time_Rendering_4th_%E4%B8%AD%E6%96%87/assets/fig_18_5_18.11.png)

**图 18.11。** Frostbite 的 CPU 作业图，内嵌图放大了其中的一小部分 [45]。（图片由 Electronic Arts 的 Johan Andersson 提供。）

### 18.5.3 基于任务的多处理

了解流水线和并行处理技术之后，自然会想到将两者结合到一个系统中。如果只有少数几个处理器可用，那么采用一个简单系统，将各子系统明确分配给某个核心，可能是合理的。但是，鉴于许多 CPU 都有大量核心，发展趋势是采用基于任务的多处理。正如可以为一个能够并行化的过程创建多个任务（也称作业）一样，这一思路还可以扩展到流水线。任何核心生成的任何任务，都会在生成时放入工作池。任何空闲处理器都会领取一个任务进行处理。

将应用程序改为多处理的一种方法，是考察其工作流程，确定哪些子系统依赖其他子系统。见图 18.11。

如果处理器在等待同步时停顿，那么由于这种成本以及任务管理的开销，应用程序的任务化版本甚至可能变得更慢 [1854]。不过，许多程序和算法确实拥有大量可以同时执行的任务，因此能够从中受益。

下一步是确定每个子系统的哪些部分可以分解成任务。一段代码若适合成为任务，通常具有以下特征 [45, 1060, 1854]：

- 任务具有明确定义的输入和输出。
- 任务在运行时是独立且无状态的，并且总能完成。
- 任务不会大到经常成为唯一仍在运行的处理过程。

C++11 等语言内置了多线程支持设施 [1445]。在 Intel 兼容系统上，Intel 的开源 Threading Building Blocks（TBB）是一个高效的库，可以简化任务生成、任务池管理和同步 [92]。

在性能至关重要时，让应用程序创建自身的任务集合，并对它们进行多处理，已是理所当然的做法，例如模拟、碰撞检测、遮挡测试和路径规划等任务 [45, 92, 1445, 1477, 1854]。这里再次指出，有些时候 GPU 核心也往往处于空闲状态。例如，在生成阴影贴图或执行深度预处理通道时，它们通常没有得到充分利用。在这些空闲时段，可以用计算着色器执行其他任务 [1313, 1884]。视架构、API 和内容而定，有时渲染流水线无法让所有着色器都保持忙碌，这意味着始终有一部分资源可用于计算着色。我们不讨论如何优化这些工作，因为 Lauritzen 提出了有说服力的论点：由于硬件差异和语言限制，无法编写既快速又可移植的计算着色器 [993]。下一小节的主题是如何优化核心渲染流水线本身。

### 18.5.4 图形 API 对多处理的支持

并行处理往往无法直接适应硬件约束。例如，DirectX 10 及更早版本一次只允许一个线程访问图形驱动程序，因此，要对实际的绘制阶段进行并行处理就比较困难 [1477]。

图形驱动程序中有两类操作可能使用多个处理器：资源创建和与渲染有关的调用。创建纹理和缓冲区等资源，可以完全是 CPU 端操作，因此天然适合并行化。不过，创建和删除也可能是阻塞任务，因为它们可能触发 GPU 上的操作，或者需要某个特定的设备上下文。无论如何，旧式 API 的诞生早于消费级多处理 CPU，因此需要重新编写，才能支持这种并发。

这里使用的一个关键构造是命令缓冲区或命令列表，其渊源可以追溯到 OpenGL 中一个称为显示列表的旧概念。命令缓冲区（command buffer，CB）是一份 API 状态变更调用和绘制调用的列表。这些列表可以按需创建、存储和重放，也可以组合起来，形成更长的命令缓冲区。只有一个 CPU 处理器通过驱动程序与 GPU 通信，因此它可以将 CB 发送给 GPU 执行。不过，每个处理器（包括这个负责通信的处理器）都可以并行地创建或串接已存储的命令缓冲区。


![图18.12：命令缓冲区](https://raw.githubusercontent.com/ahuibo/Real-Time-Rendering-4th-CN/main/Real-Time_Rendering_4th_%E4%B8%AD%E6%96%87/assets/fig_18_5_18.12.png)

**图 18.12。** 命令缓冲区。每个处理器使用自己的延迟上下文（橙色），创建并填充一个或多个命令缓冲区（蓝色）。每个命令缓冲区都会发送到进程 #1，由该进程使用自己的立即上下文（绿色）按需执行。进程 #1 在等待来自进程 #3 的命令缓冲区 N 时，可以执行其他操作。（据 Zink 等人 [1971] 改编。）

例如，在 DirectX 11 中，与驱动程序通信的处理器会将渲染调用发送到所谓的立即上下文（immediate context）。其他处理器各自使用一个延迟上下文（deferred context）生成命令缓冲区。顾名思义，这些命令不会直接发送到驱动程序，而是发送到立即上下文进行渲染。见图 18.12。另一种做法是将一个命令缓冲区发送到另一个延迟上下文，由后者将其插入自己的 CB。除了将命令缓冲区发送到驱动程序执行之外，立即上下文能够执行而延迟上下文不能执行的主要操作，是 GPU 查询和数据回读。除此之外，从这两类上下文来看，命令缓冲区的管理方式相同。

命令缓冲区及其前身——显示列表——的一个优点，是可以存储并重放。命令缓冲区在创建时并未完成全部绑定，这有助于复用。例如，假设某个 CB 包含一个观察矩阵。相机移动了，观察矩阵也随之变化。不过，观察矩阵存储在常量缓冲区中。CB 并不存储常量缓冲区的内容，只存储对它们的引用。因此，可以更改常量缓冲区的内容，而无须重建 CB。要确定如何最好地提高并行性，就需要为命令缓冲区的创建、存储和组合选择合适的粒度——按视图、按对象，还是按材质 [1971]。

在命令缓冲区成为现代 API 的一部分之前，这类多线程绘制系统就已经存在多年 [1152, 1349, 1552, 1554]。API 支持使这一过程更简单，也使更多工具能够配合所创建的系统工作。然而，命令列表确实会带来创建和内存成本。此外，如第 18.4.2 节所述，在 DirectX 11 和 OpenGL 中，将 API 的状态设置映射到底层 GPU，仍是一项开销很大的操作。在这些系统中，当应用程序是瓶颈时，命令缓冲区可能有帮助；但当驱动程序是瓶颈时，它们可能适得其反。

这些较早的 API 中，某些语义不允许驱动程序将各种操作并行化，这推动了 Vulkan、DirectX 12 和 Metal 的开发。这些较新的 API 采用精简的绘制提交接口，能够很好地映射到现代 GPU，从而最大限度地降低驱动程序成本。命令缓冲区管理、内存分配和同步决策，都从驱动程序的责任变成应用程序的责任。此外，在这些较新的 API 中，命令缓冲区在形成时只验证一次，所以重复重放的开销比 DirectX 11 等较早 API 使用的命令缓冲区更低。所有这些因素共同提高了 API 效率，实现了多处理，并减少了驱动程序成为瓶颈的可能性。


## 第18章 延伸阅读与资源

来源：原书书页 814—815（PDF 第 835—836 页），“Further Reading and Resources”及其全部后续文字；PDF 第 837 页仅有出版商标志及网址，记录于文末，下一章从 PDF 第 838 页开始。原文标题未编号，此处文件名中的 18.99 仅用于合并排序。

移动设备在各项工作耗时的分配上，可能有不同的平衡，尤其是在使用基于分块的架构时。Merry [1200] 讨论了这些成本，以及如何有效使用这类 GPU。Pranckevičius 和 Zioma [1433] 的报告深入介绍了移动设备优化的许多方面。McCaffrey [1156] 比较了移动与桌面架构及其性能特征。像素着色通常是移动 GPU 上最大的开销。Sathe [1545] 和 Etuaho [443] 讨论了移动设备上的着色器精度问题和优化。

对于桌面平台，Wiesendanger [1882] 全面介绍了现代游戏引擎的架构。O’Donnell [1313] 展示了基于图的渲染系统的优点。Zink 等人 [1971] 深入讨论了 DirectX 11。De Smedt [331] 针对电子游戏中常见的性能热点提供了指导，其中包括 DirectX 11 和 12、多 GPU 配置以及虚拟现实的优化。Coombes [291] 概述了 DirectX 12 的最佳实践，Kubisch [946] 则提供了关于何时应使用 Vulkan 的指南。关于从旧式 API 移植到 DirectX 12 和 Vulkan，已经有大量报告 [249, 536, 699, 1438]。当你读到这里时，无疑还会有更多。可以查看 NVIDIA、AMD 和 Intel 等独立硬件供应商（IHV）的开发者网站、Khronos Group 的网站、互联网上的其他资源，以及本书网站。

虽然年代稍早，Cebenoyan 的文章 [240] 仍然具有参考价值。它概述了如何查找瓶颈，以及提高效率的技术。Fog [476] 和 Isensee [801] 的 C++ 优化指南颇受欢迎，可以在网上免费获取。Hughes 等人 [783] 以现代视角深入讨论了如何使用跟踪工具和 GPUView，分析瓶颈发生的位置。虽然重点是虚拟现实系统，但其中讨论的技术适用于任何基于 Windows 的机器。

Sutter [1725] 讨论了 CPU 时钟频率如何趋于平缓，以及多处理器芯片组如何兴起。关于发生这一变化的更多原因，以及芯片如何设计的信息，可参阅 Asanovic 等人 [75] 的深入报告。Foley [478] 在图形应用程序开发的背景下，讨论了多种形式的并行性。《Game Engine Gems 2》[1024] 收录了数篇介绍如何为游戏引擎编写多线程部分的文章。Preshing [1445] 解释了 Ubisoft 如何使用多线程，并具体说明如何使用 C++11 的线程支持。Tatarchuk [1749, 1750] 的两场报告详细介绍了游戏《Destiny》使用的多线程架构和着色流水线。

出版商隔页（PDF 第 837 页）：Taylor & Francis（泰勒与弗朗西斯）；Taylor & Francis Group（泰勒与弗朗西斯集团）；网址：[http://taylorandfrancis.com](http://taylorandfrancis.com)。
