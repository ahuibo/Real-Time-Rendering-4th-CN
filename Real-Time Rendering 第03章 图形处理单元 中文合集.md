# Real-Time Rendering 第03章 图形处理单元 中文合集

> 依据用户提供的《Real-Time Rendering》第四版 PDF 完整翻译。原图配中文图注；复杂数学已排版为本地图片，简单符号直接显示。保留原书技术年代、公式编号与引用编号。

## 目录

- [3.1 数据并行架构](<Real-Time_Rendering_4th_中文/第03章/03.01_数据并行架构.md>)

- [3.2 GPU流水线概览](<Real-Time_Rendering_4th_中文/第03章/03.02_GPU流水线概览.md>)

- [3.3 可编程着色器阶段](<Real-Time_Rendering_4th_中文/第03章/03.03_可编程着色器阶段.md>)

- [3.4 可编程着色与API的演进](<Real-Time_Rendering_4th_中文/第03章/03.04_可编程着色与API的演进.md>)

- [3.5 顶点着色器](<Real-Time_Rendering_4th_中文/第03章/03.05_顶点着色器.md>)

- [3.6 曲面细分阶段](<Real-Time_Rendering_4th_中文/第03章/03.06_曲面细分阶段.md>)

- [3.7 几何着色器](<Real-Time_Rendering_4th_中文/第03章/03.07_几何着色器.md>)

- [3.8 像素着色器](<Real-Time_Rendering_4th_中文/第03章/03.08_像素着色器.md>)

- [3.9 合并阶段](<Real-Time_Rendering_4th_中文/第03章/03.09_合并阶段.md>)

- [3.10 计算着色器](<Real-Time_Rendering_4th_中文/第03章/03.10_计算着色器.md>)


## 章首导言

> 来源：Real-Time Rendering, Fourth Edition，书页 29—30（PDF 第50—51页）。

> “显示器就是计算机。”
>
> ——黄仁勋（Jen-Hsun Huang）

从历史上看，图形加速最初是对与三角形重叠的每条像素扫描线上的颜色进行插值，然后显示这些值。加入访问图像数据的能力后，就能够把纹理应用到表面上。再加入用于对 ![数学符号](https://raw.githubusercontent.com/ahuibo/Real-Time-Rendering-4th-CN/main/Real-Time_Rendering_4th_%E4%B8%AD%E6%96%87/assets/math/eq_03_00_00efa9f788eb2b.png) 深度进行插值和测试的硬件，就能提供内置的可见性检查。由于这些处理过程使用频繁，人们将其交由专用硬件执行，以提高性能。在随后各代硬件中，渲染流水线的更多部分被加入进来，各部分的功能也大幅增加。专用图形硬件相对于 CPU 唯一的计算优势是速度，但速度至关重要。

在过去二十年中，图形硬件经历了令人难以置信的变化。首款包含硬件顶点处理功能的消费级图形芯片——NVIDIA 的 GeForce 256——于 1999 年出货。NVIDIA 创造了“图形处理单元”（graphics processing unit，GPU）这一术语，用来区分 GeForce 256 与此前只能进行光栅化的芯片，而这个名称也沿用下来。在接下来的几年里，GPU 从复杂固定功能流水线的可配置实现，演变成高度可编程的平台，开发者可以在上面实现自己的算法。各种可编程着色器成为控制 GPU 的主要手段。为提高效率，流水线的某些部分仍然只能配置、不能编程，但总体趋势是走向可编程性与灵活性 [175]。

GPU 通过专注于一组范围较窄、能够高度并行化的任务，获得了极高的速度。例如，它们具有专门实现 ![数学符号](https://raw.githubusercontent.com/ahuibo/Real-Time-Rendering-4th-CN/main/Real-Time_Rendering_4th_%E4%B8%AD%E6%96%87/assets/math/eq_03_00_00efa9f788eb2b.png) 缓冲、快速访问纹理图像和其他缓冲区，以及确定哪些像素被三角形覆盖的定制电路。这些部件如何完成各自功能，将在第 23 章介绍。在一开始，更重要的是了解 GPU 如何让其可编程着色器实现并行。

第 3.3 节解释着色器如何工作。目前，你需要知道的是：着色器核心是一个小型处理器，用来完成某种相对独立的任务，例如把顶点从世界中的位置变换为屏幕坐标，或者计算被三角形覆盖的像素的颜色。由于每帧都有成千上万乃至数百万个三角形被送往屏幕，每秒可能发生数十亿次着色器调用，也就是数十亿个分别运行着色器程序的实例。

首先，延迟是所有处理器都面临的问题。访问数据需要一定时间。理解延迟的一种基本方式是：信息离处理器越远，等待就越久。第 23.3 节将更详细地讨论延迟。访问存储在内存芯片中的信息，比访问本地寄存器中的信息耗时更长。第 18.4.1 节会更深入地讨论内存访问。关键在于，等待数据取回意味着处理器处于停顿状态，从而降低性能。


## 3.1 数据并行架构

来源：原书书页 30—33（PDF 物理页 51—54）。

不同的处理器架构采用各种策略来避免停顿。CPU 针对处理种类繁多的数据结构和庞大的代码库进行了优化。CPU 可以包含多个处理器，但每个处理器主要以串行方式运行代码，有限的 SIMD 向量处理只是一个小小的例外。为了尽量减小延迟的影响，CPU 芯片的很大一部分由快速的本地缓存组成，这些存储器中填充着接下来可能需要的数据。CPU 还通过分支预测、指令重排、寄存器重命名和缓存预取等巧妙的技术来避免停顿 [715]。

GPU 采用另一种方法。GPU 芯片的很大一部分面积用于容纳大量处理器，称为**着色器核心**，其数量通常达到数千个。GPU 是一种流处理器，其中由相似数据组成的有序集合会依次得到处理。正因为这些数据具有相似性，例如一组顶点或像素，GPU 才能够以大规模并行的方式处理它们。另一个重要条件是，这些调用要尽可能彼此独立，使它们既不需要相邻调用的信息，也不共享可写的内存位置。有时，为了实现新的有用功能，也会打破这条规则，但这类例外的代价是可能产生延迟，因为一个处理器可能需要等待另一个处理器完成工作。

GPU 针对**吞吐量**进行了优化，吞吐量定义为能够处理数据的最大速率。不过，这种快速处理是有代价的。由于分配给缓存存储器和控制逻辑的芯片面积较少，每个着色器核心遇到的延迟通常明显高于 CPU 处理器遇到的延迟 [462]。

假设一个网格被光栅化后，有两千个像素包含需要处理的片元，那么像素着色器程序就要被调用两千次。设想只有一个着色器处理器，也就是世界上最弱的 GPU。它开始为这两千个片元中的第一个片元执行着色器程序。着色器处理器先对寄存器中的值进行几次算术运算。寄存器位于本地，访问很快，因此不会发生停顿。随后，着色器处理器遇到一条指令，例如纹理访问指令：对于某个给定的表面位置，程序需要知道贴在网格上的图像在该处的像素颜色。纹理是完全独立的资源，不属于像素程序的本地存储器，而且纹理访问可能相当复杂。一次内存读取可能需要数百到数千个时钟周期，在这段时间里，GPU 处理器无事可做。此时，着色器处理器就会停顿，等待纹理的颜色值返回。

为了让这个糟糕的 GPU 变得好得多，可以给每个片元分配一小块存储空间，用于保存它的本地寄存器。这样，着色器处理器在读取纹理时就不必停顿，而是可以切换去执行另一个片元，即两千个片元中的第二个。这种切换极快，除了记下第一个片元当时正在执行哪条指令以外，第一个和第二个片元中的任何内容都不受影响。现在执行第二个片元。与第一个片元一样，先执行几个算术函数，然后又遇到一次纹理读取。着色器核心于是切换到另一个片元，也就是第三个。最终，全部两千个片元都以这种方式得到处理。此时，着色器处理器返回第一个片元。到这时，纹理颜色已经取回并可供使用，因此着色器程序就能继续执行。处理器以同样的方式继续运行，直到遇到另一条已知会导致执行停顿的指令，或者程序执行完毕。与着色器处理器一直专注于单个片元的情况相比，单个片元完成执行所需的时间会更长，但全部片元的总体执行时间却会大幅缩短。

在这种架构中，GPU 通过切换到另一个片元来保持忙碌，从而隐藏延迟。GPU 在这一设计的基础上更进一步，将指令执行逻辑与数据分离。这种安排称为**单指令多数据**（single instruction, multiple data，SIMD），它对固定数量的着色器程序同步执行同一条命令。与为每个程序配备独立的逻辑和分派单元相比，SIMD 的优势在于，用于数据处理和切换的硅芯片资源以及功耗都少得多。用现代 GPU 的术语来表述前面两千个片元的例子，为一个片元进行的每次像素着色器调用都称为一个**线程**。这种线程不同于 CPU 线程。它由一小块用于保存着色器输入值的存储空间，以及着色器执行所需的寄存器空间组成。使用相同着色器程序的线程会被编组成组，NVIDIA 将这些组称为 **warp（线程束）**，AMD 则称之为 **wavefront（波前）**。一个线程束或波前会被调度到一定数量的 GPU 着色器核心上，通过 SIMD 处理来执行，核心数量介于 8 到 64 之间。每个线程映射到一个 **SIMD 通道**。

假设我们有两千个待执行的线程。NVIDIA GPU 上的线程束包含 32 个线程。因此，线程束的数量为 2000 ÷ 32 = 62.5，这意味着需要分配 63 个线程束，其中一个有一半是空的。线程束的执行与前面单个 GPU 处理器的例子相似。着色器程序在全部 32 个处理器上同步执行。遇到内存读取时，所有线程都会同时遇到它，因为所有线程执行的是同一条指令。这次读取表明，该线程束中的线程即将停顿，所有线程都要等待各自不同的结果。此时并不真正停顿，而是换出当前线程束，换入另一个包含 32 个线程的线程束，再由这 32 个核心执行。切换速度与单处理器系统的例子一样快，因为换入或换出线程束时，并不触及各个线程中的数据。每个线程都有自己的寄存器，每个线程束也会记录自己正在执行哪条指令。换入一个新的线程束，只需让这一组核心转而执行另一组线程，没有其他开销。各线程束不断执行或换出，直到全部执行完成。见图 3.1。


![图 3.1：简化的着色器执行示例](https://raw.githubusercontent.com/ahuibo/Real-Time-Rendering-4th-CN/main/Real-Time_Rendering_4th_%E4%B8%AD%E6%96%87/assets/fig_3.1_3.1.png)

**图 3.1**　简化的着色器执行示例。一个三角形的片元在这里称为线程，它们被编入线程束。图中每个线程束画成包含 4 个线程，而实际上包含 32 个线程。要执行的着色器程序共有 5 条指令。由 4 个 GPU 着色器处理器组成的处理器组，为第一个线程束执行这些指令，直到在 `txr` 命令处检测到停顿条件；该命令需要时间来取回数据。随后换入第二个线程束，对它执行着色器程序的前 3 条指令，直到再次检测到停顿。第三个线程束被换入并发生停顿后，再换入第一个线程束，继续执行。如果此时它的 `txr` 命令所需的数据尚未返回，执行才会真正停顿，直到这些数据可用。各个线程束依次完成。

在这个简单例子中，读取纹理所需的内存访问延迟可能导致线程束被换出。实际上，由于切换的代价很低，即使延迟更短，也可能换出线程束。还有其他一些用于优化执行的技术 [945]，但线程束切换是所有 GPU 用来隐藏延迟的主要机制。这一过程的效率受到若干因素影响。例如，如果线程很少，能够创建的线程束也就很少，隐藏延迟就会变得困难。

着色器程序的结构是影响效率的一项重要特征。其中一个主要因素是每个线程使用的寄存器数量。在前面的例子中，我们假定两千个线程能够同时驻留在 GPU 上。每个线程所关联的着色器程序需要的寄存器越多，能够驻留在 GPU 中的线程就越少，因而线程束也越少。线程束不足可能意味着无法通过切换来缓解停顿。驻留的线程束称为处于“在途”（in flight）状态，这个数量称为**占用率**（occupancy）。高占用率意味着有许多线程束可供处理，因此处理器空闲的可能性较小。低占用率往往会导致较差的性能。内存读取的频率也会影响需要隐藏多少延迟。Lauritzen [993] 概述了着色器使用的寄存器数量和共享内存如何影响占用率。Wronski [1911, 1914] 讨论了理想占用率会如何随着色器所执行的操作类型而变化。

另一个影响整体效率的因素是由 `if` 语句和循环引起的**动态分支**。假设着色器程序遇到一个 `if` 语句。如果所有线程求值后都进入同一个分支，线程束就可以继续执行，不必考虑另一个分支。但如果部分线程，哪怕只有一个线程，选择了另一条路径，那么线程束就必须执行两个分支，并丢弃各个线程不需要的结果 [530, 945]。这一问题称为**线程分歧**：少数线程可能需要执行一次循环迭代，或执行一条 `if` 路径，而线程束中的其他线程不需要这样做，导致其他线程在这段时间里空闲。

所有 GPU 都实现了这些架构思想，形成了具有严格限制、但每瓦功耗能提供巨大计算能力的系统。作为程序员，理解这一系统如何运作，将帮助你更高效地利用它提供的算力。在接下来的各节中，我们将讨论 GPU 如何实现渲染流水线、可编程着色器如何工作，以及 GPU 各个阶段的演变和功能。


## 3.2 GPU流水线概览

来源：原书书页 34（PDF 第 55 页）；本节截至书页 35 的第 3.3 节标题之前。

GPU 实现了第 2 章所描述的概念上的几何处理、光栅化和像素处理流水线阶段。这些阶段被划分为若干硬件阶段，各自具有不同程度的可配置性或可编程性。图 3.2 用不同颜色标出了各个阶段的可编程或可配置程度。请注意，这些物理阶段的划分与第 2 章介绍的功能阶段略有不同。


![图 3.2 渲染流水线的 GPU 实现](https://raw.githubusercontent.com/ahuibo/Real-Time-Rendering-4th-CN/main/Real-Time_Rendering_4th_%E4%B8%AD%E6%96%87/assets/fig_3.2_3.2.png)

**图 3.2** 渲染流水线的 GPU 实现。各阶段按照用户对其操作的控制程度用不同颜色表示。绿色阶段完全可编程。虚线表示可选阶段。黄色阶段可配置，但不可编程，例如，可以为合并阶段设置各种混合模式。蓝色阶段的功能完全固定。

这里描述的是 GPU 的*逻辑模型*，即 API 向作为程序员的你呈现的模型。如第 18 章和第 23 章所讨论的，这条逻辑流水线的实现，也就是*物理模型*，由硬件厂商决定。在逻辑模型中功能固定的某个阶段，可能通过向相邻的可编程阶段添加命令而在 GPU 上执行。流水线中的单个程序可能被拆成若干部分，由不同的子单元执行，也可能完全通过单独的一遍处理来执行。逻辑模型可以帮助你推断哪些因素会影响性能，但不应将其误认为 GPU 实际实现流水线的方式。

顶点着色器是一个完全可编程的阶段，用于实现几何处理阶段。几何着色器也是一个完全可编程的阶段，它操作的是一个图元（点、线或三角形）的各个顶点。它可以用于执行逐图元的着色操作、销毁图元，或创建新图元。曲面细分阶段和几何着色器都是可选的，并非所有 GPU 都支持它们，尤其是在移动设备上。

裁剪、三角形设置和三角形遍历阶段由固定功能硬件实现。屏幕映射受窗口和视口设置的影响，在内部形成简单的缩放和位置调整。像素着色器阶段完全可编程。合并阶段虽然不可编程，但具有很高的可配置性，可以设置为执行多种不同的操作。它实现了“合并”这一功能阶段，负责修改颜色缓冲区、z 缓冲区、混合缓冲区、模板缓冲区，以及任何其他与输出相关的缓冲区。像素着色器的执行与合并阶段一起，构成了第 2 章介绍的概念上的像素处理阶段。

随着时间推移，GPU 流水线逐渐摆脱硬编码操作，朝着更高的灵活性和更强的控制能力发展。可编程着色器阶段的引入，是这一演进过程中最重要的一步。下一节将介绍各个可编程阶段共有的特性。


## 3.3 可编程着色器阶段

原书来源：书页 35—37（PDF 物理页 56—58）。

现代着色器程序采用统一着色器设计。这意味着顶点着色器、像素着色器、几何着色器以及与曲面细分相关的着色器共享同一种编程模型。在内部，它们具有相同的指令集架构（instruction set architecture，ISA）。实现这一模型的处理器在 DirectX 中称为通用着色器核心（common-shader core），而具有这类核心的 GPU 则被称为采用了统一着色器架构。这类架构背后的思想是：着色器处理器可以承担多种不同的角色，GPU 能够根据需要对其进行分配。例如，相比于每个仅由两个三角形组成的大正方形，一组包含许多微小三角形的网格需要更多的顶点着色器处理。如果 GPU 将顶点着色器核心和像素着色器核心分成彼此独立的资源池，那么，要使所有核心都保持忙碌，所需的理想工作分配比例就已经被固定下来。使用统一着色器核心后，GPU 可以自行决定如何平衡这些负载。

完整介绍着色器编程模型远远超出了本书的范围，而且已经有许多文档、书籍和网站对此进行了介绍。着色器使用类似 C 的着色语言编写，例如 DirectX 的高级着色语言（High-Level Shading Language，HLSL）和 OpenGL 着色语言（OpenGL Shading Language，GLSL）。DirectX 的 HLSL 可以编译成虚拟机字节码，也称为中间语言（intermediate language，IL 或 DXIL），从而实现硬件无关性。中间表示还可以使着色器程序能够离线编译和存储。驱动程序会将这种中间语言转换成特定 GPU 的 ISA。游戏主机编程通常会省去中间语言这一步，因为该系统只有一种 ISA。

基本数据类型是 32 位单精度浮点标量和向量。不过，如前文所述，向量只是着色器代码中的一部分，硬件本身并不支持向量。现代 GPU 还原生支持 32 位整数和 64 位浮点数。浮点向量通常包含位置（xyzw）、法线、矩阵的行、颜色（rgba）或纹理坐标（uvwq）等数据。整数最常用来表示计数器、索引或位掩码。结构体、数组和矩阵等聚合数据类型也受到支持。

绘制调用（draw call）通过调用图形 API 来绘制一组图元，从而使图形流水线执行并运行其中的着色器。每个可编程着色器阶段都有两类输入：一致输入（uniform input），其值在一次绘制调用期间保持不变，但可以在不同绘制调用之间改变；变化输入（varying input），其数据来自三角形的顶点或光栅化过程。例如，像素着色器可以将光源颜色作为一致值，而三角形表面上的位置会逐像素变化，因此属于变化值。纹理是一种特殊的一致输入：过去，它始终是应用于表面的彩色图像，而如今可以将它视为任意大型数据数组。

底层虚拟机为不同类型的输入和输出提供专用寄存器。可供一致输入使用的常量寄存器数量，远多于可供变化输入或输出使用的寄存器数量。这是因为变化输入和输出需要为每个顶点或像素分别存储，因此所需的数量自然存在一个上限。一致输入只存储一次，并在一次绘制调用的所有顶点或像素之间重复使用。虚拟机还具有通用的临时寄存器，用作暂存空间。所有类型的寄存器都可以使用临时寄存器中的整数值进行数组索引。图 3.3 展示了着色器虚拟机的输入和输出。


![图 3.3：统一着色器虚拟机架构及寄存器布局](https://raw.githubusercontent.com/ahuibo/Real-Time-Rendering-4th-CN/main/Real-Time_Rendering_4th_%E4%B8%AD%E6%96%87/assets/fig_3.3_3.3.png)

**图 3.3**　在 Shader Model 4.0 下的统一虚拟机架构和寄存器布局。每种资源旁均标出了可用数量的最大值。以斜杠分隔的三个数字，从左到右分别表示顶点着色器、几何着色器和像素着色器的限制。

现代 GPU 能够高效执行图形计算中常见的运算。着色语言通过 `*` 和 `+` 等运算符提供最常用的运算，例如加法和乘法。其余运算则通过针对 GPU 优化的内建函数提供，例如 `atan()`、`sqrt()`、`log()` 以及许多其他函数。对于更复杂的运算，也有相应的函数可用，例如向量归一化与反射、叉积，以及矩阵转置和行列式计算。

流程控制（flow control）是指使用分支指令改变代码的执行流程。与流程控制相关的指令用于实现高级语言中的 `if`、`case` 语句等结构，以及各种类型的循环。着色器支持两种流程控制。静态流程控制的分支基于一致输入的值。这意味着在一次绘制调用期间，代码的执行流程保持不变。静态流程控制的主要好处是允许同一个着色器用于多种不同情形，例如不同数量的光源。由于所有着色器调用都走相同的代码路径，因此不会出现线程分歧。动态流程控制基于变化输入的值，这意味着每个片元都可以以不同的方式执行代码。它比静态流程控制强大得多，但可能会付出性能代价，尤其是当不同着色器调用之间的代码执行流程变化不规则时。


## 3.4 可编程着色与API的演进

来源：《Real-Time Rendering, Fourth Edition》，书页 37—41（PDF 物理页 58—62）。

可编程着色框架的思想可以追溯到 1984 年 Cook 提出的着色树（shade trees）[287]。图 3.4 展示了一个简单的着色器及其对应的着色树。RenderMan 着色语言 [63, 1804] 于 20 世纪 80 年代末从这一思想发展而来。时至今日，它仍与其他不断演进的规范一起用于电影制作中的渲染，例如开放着色语言（Open Shading Language，OSL）项目 [608]。


![图 3.4 简单铜材质着色器的着色树与程序](https://raw.githubusercontent.com/ahuibo/Real-Time-Rendering-4th-CN/main/Real-Time_Rendering_4th_%E4%B8%AD%E6%96%87/assets/fig_3_4_3.4.png)

图 3.4. 一个简单铜材质着色器的着色树，以及与之对应的着色语言程序。（据 Cook [287] 改编。）

消费级图形硬件最早由 3dfx Interactive 于 1996 年 10 月 1 日成功推出。图 3.5 给出了从这一年开始的时间线。其 Voodoo 显卡能够以高质量和高性能渲染游戏《Quake》，因此迅速得到采用。这种硬件从头到尾实现的都是固定功能流水线。在 GPU 原生支持可编程着色器之前，人们曾多次尝试通过多个渲染通道实时实现可编程着色操作。1999 年，《Quake III: Arena》的脚本语言成为这一领域首个得到广泛应用的商业成功案例。正如本章开头所述，NVIDIA 的 GeForce256 是首款被称为 GPU 的硬件，但它并不可编程。不过，它是可配置的。


![图 3.5 部分 API 与图形硬件发布时间线](https://raw.githubusercontent.com/ahuibo/Real-Time-Rendering-4th-CN/main/Real-Time_Rendering_4th_%E4%B8%AD%E6%96%87/assets/fig_3_4_3.5.png)

图 3.5. 部分 API 和图形硬件的发布时间线。

2001 年初，NVIDIA 的 GeForce 3 成为首款支持可编程顶点着色器的 GPU [1049]，这些功能通过 DirectX 8.0 和 OpenGL 扩展提供。这些着色器使用一种类似汇编的语言编写，由驱动程序即时转换为微代码。DirectX 8.0 也包含像素着色器，但这些像素着色器还算不上真正可编程：它们所支持的有限“程序”会由驱动程序转换成纹理混合状态，再由这些状态把硬件“寄存器组合器”连接起来。这些“程序”不仅长度有限（最多 12 条指令），还缺少重要功能。Peercy 等人 [1363] 通过研究 RenderMan，确定了依赖纹理读取和浮点数据对于真正的可编程性至关重要。

当时的着色器不允许使用流程控制（分支），因此必须把条件判断的两个分支都计算出来，再选择其中一个结果或在两者之间插值，以此模拟条件判断。DirectX 定义了着色器模型（Shader Model，SM）这一概念，用来区分具有不同着色器能力的硬件。2002 年，包含着色器模型 2.0 的 DirectX 9.0 发布，提供了真正可编程的顶点着色器和像素着色器。OpenGL 也通过各种扩展提供了类似功能。新版本加入了对任意依赖纹理读取以及 16 位浮点值存储的支持，终于满足了 Peercy 等人确定的全部要求。指令、纹理和寄存器等着色器资源的上限得到提高，使着色器能够实现更复杂的效果。此外，还加入了对流程控制的支持。着色器的长度和复杂度不断增加，使汇编编程模型变得越来越繁琐。幸运的是，DirectX 9.0 还包含了 HLSL。这种着色语言由 Microsoft 与 NVIDIA 合作开发。大约在同一时期，OpenGL ARB（架构审查委员会，Architecture Review Board）发布了 GLSL，这是一种用于 OpenGL、与 HLSL 相当类似的语言 [885]。这些语言深受 C 编程语言的语法和设计理念影响，也包含了来自 RenderMan 着色语言的元素。

着色器模型 3.0 于 2004 年推出，增加了动态流程控制，大幅增强了着色器的能力。它还把可选功能变成了必需功能，进一步提高了资源上限，并为顶点着色器中的纹理读取增加了有限支持。当新一代游戏主机分别于 2005 年末（Microsoft 的 Xbox 360）和 2006 年末（Sony Computer Entertainment 的 PLAYSTATION 3 系统）推出时，它们都配备了着色器模型 3.0 级别的 GPU。Nintendo 的 Wii 主机于 2006 年末首次发售，其 GPU 是最后一批引人注目的固定功能 GPU 之一。到如今，纯粹的固定功能流水线早已成为过去。着色器语言的发展已达到可以使用多种工具来创建和管理它们的程度。图 3.6 展示了其中一种工具的屏幕截图，它采用了 Cook 的着色树概念。


![图 3.6 用于着色器设计的可视化着色器图系统](https://raw.githubusercontent.com/ahuibo/Real-Time-Rendering-4th-CN/main/Real-Time_Rendering_4th_%E4%B8%AD%E6%96%87/assets/fig_3_4_3.6.png)

图 3.6. 一个用于着色器设计的可视化着色器图系统。各种操作被封装在功能框中，可以从左侧选择。选中后，每个功能框都有可调整的参数，显示在右侧。各功能框的输入和输出相互连接，形成最终结果；结果显示在中间窗格的右下方。（截图来自 mental images 公司的“mental mill”。）

可编程性方面的下一次重大进步同样出现在 2006 年末前后。DirectX 10.0 [175] 所包含的着色器模型 4.0 引入了若干重要功能，例如几何着色器和流输出。着色器模型 4.0 为所有着色器（顶点、像素和几何着色器）提供了统一的编程模型，也就是前文所述的统一着色器设计。资源上限进一步提高，同时加入了对整数数据类型（包括按位运算）的支持。OpenGL 3.3 中引入的 GLSL 3.30 提供了类似的着色器模型。

2009 年，DirectX 11 和着色器模型 5.0 发布，增加了曲面细分阶段的着色器和计算着色器，后者也称为 DirectCompute。这一版本还着重更有效地支持 CPU 多处理，这个主题将在第 18.5 节讨论。OpenGL 在 4.0 版本中增加了曲面细分，在 4.3 版本中增加了计算着色器。

DirectX 和 OpenGL 的演进方式不同。两者都会为特定版本设定所需的硬件支持级别。Microsoft 掌控着 DirectX API，因此会直接与 AMD、NVIDIA、Intel 等独立硬件厂商（IHV），以及游戏开发商和计算机辅助设计软件公司合作，决定开放哪些功能。OpenGL 由硬件和软件厂商组成的联盟开发，并由非营利组织 Khronos Group 管理。由于参与公司众多，API 功能往往在 DirectX 中引入一段时间之后，才会出现在某个 OpenGL 版本中。不过，OpenGL 允许使用厂商专有的或更通用的扩展，使最新的 GPU 功能能够在正式版本提供支持之前就得到使用。

API 的下一次重大变革由 AMD 于 2013 年推出 Mantle API 引领。Mantle 与电子游戏开发商 DICE 合作开发，其理念是去掉图形驱动程序的大量开销，并将这部分控制权直接交给开发者。在进行这种重构的同时，它还进一步支持高效的 CPU 多处理。这一新类别的 API 着重于大幅减少 CPU 花费在驱动程序中的时间，并为 CPU 多处理器提供更高效的支持（第 18 章）。Microsoft 采纳了 Mantle 开创的理念，并于 2015 年以 DirectX 12 的形式发布。注意，DirectX 12 并不以开放新的 GPU 功能为重点：DirectX 11.3 开放了相同的硬件功能。这两个 API 都可以用来向 Oculus Rift 和 HTC Vive 等虚拟现实系统发送图形。不过，DirectX 12 对 API 进行了彻底的重新设计，使其能更好地映射到现代 GPU 架构。对于 CPU 端驱动程序开销造成瓶颈的应用，或者能通过使用更多 CPU 处理器执行图形工作来提高性能的应用，低开销驱动程序很有用 [946]。从较早的 API 移植过来可能很困难，而简单直接的实现反而可能导致性能下降 [249, 699, 1438]。

Apple 于 2014 年发布了自己的低开销 API，名为 Metal。Metal 最初可用于 iPhone 5S 和 iPad Air 等移动设备，一年后，较新的 Macintosh 计算机通过 OS X El Capitan 也获得了支持。除了提高效率，减少 CPU 使用量还能节省电能，这在移动设备上是一个重要因素。这个 API 有自己的着色语言，同时面向图形程序和 GPU 计算程序。

AMD 将其 Mantle 的开发成果捐赠给了 Khronos Group，后者于 2016 年初发布了自己的新 API，名为 Vulkan。与 OpenGL 一样，Vulkan 可以在多种操作系统上运行。Vulkan 使用一种名为 SPIR-V 的新高级中间语言，它既用于表示着色器，也用于通用 GPU 计算。预编译的着色器具有可移植性，因此可以用于任何支持其所需能力的 GPU [885]。Vulkan 也可用于非图形的 GPU 计算，因为它不需要显示窗口 [946]。Vulkan 与其他低开销驱动程序之间的一个显著区别是，它旨在适用于广泛的系统，从工作站到移动设备都包含在内。

在移动设备上，通常使用的是 OpenGL ES。“ES”是嵌入式系统（Embedded Systems）的缩写，因为这一 API 在开发时就考虑到了移动设备。当时，标准 OpenGL 相当臃肿，其某些调用结构也比较缓慢，还要求支持很少使用的功能。OpenGL ES 1.0 于 2003 年发布，是 OpenGL 1.3 的精简版本，描述的是固定功能流水线。虽然 DirectX 的发布时机与支持它的图形硬件相配合，但移动设备图形支持的发展并不是以同样的方式进行的。例如，2010 年发布的第一代 iPad 实现的是 OpenGL ES 1.1。2007 年，OpenGL ES 2.0 规范发布，提供了可编程着色功能。它基于 OpenGL 2.0，但不包含固定功能部分，因此不向后兼容 OpenGL ES 1.1。OpenGL ES 3.0 于 2012 年发布，提供了多渲染目标、纹理压缩、变换反馈、实例化以及种类丰富得多的纹理格式和模式等功能，还改进了着色器语言。OpenGL ES 3.1 增加了计算着色器，3.2 则增加了几何着色器、曲面细分着色器及其他功能。第 23 章将更详细地讨论移动设备架构。

OpenGL ES 的一个分支是基于浏览器的 API WebGL，它通过 JavaScript 调用。这个 API 的第一个版本于 2011 年发布，可以在大多数移动设备上使用，因为其功能等同于 OpenGL ES 2.0。与 OpenGL 一样，扩展可以让开发者使用更高级的 GPU 功能。WebGL 2 以支持 OpenGL ES 3.0 为前提。

WebGL 特别适合用于功能实验或课堂教学：

- 它是跨平台的，能够在所有个人计算机和几乎所有移动设备上运行。
- 驱动程序的认可由浏览器负责。即使某个浏览器不支持特定的 GPU 或扩展，另一个浏览器通常也会支持。
- 代码采用解释执行而非编译执行，开发时只需要一个文本编辑器。
- 大多数浏览器都内置调试器，而且可以查看任何网站上运行的代码。
- 例如，可以通过把程序上传到网站或 Github 来部署程序。

three.js [218] 等更高层次的场景图与效果库，让人们能够方便地使用各种较复杂效果的代码，例如阴影算法、后处理效果、基于物理的着色以及延迟渲染。


## 3.5 顶点着色器

原书来源：书页 42—44（PDF 物理页 63—65）。

顶点着色器是图 3.2 所示功能流水线中的第一个阶段。虽然这是程序员能够直接控制的第一个阶段，但需要注意，在此之前已经进行了一些数据操作。在 DirectX 称为*输入装配器*（input assembler）的部分中 [175, 530, 1208]，可以将多个数据流交织在一起，形成送入流水线的顶点集合和图元集合。例如，一个物体可以用一个位置数组和一个颜色数组来表示。输入装配器通过创建带有位置和颜色的顶点，构造出这个物体的三角形（或者线、点）。另一个物体可以使用同一个位置数组（配合不同的模型变换矩阵）以及不同的颜色数组来表示。第 16.4.5 节将详细讨论数据表示。输入装配器还支持*实例化*（instancing）。它允许通过一次绘制调用将一个物体绘制多次，并让各个实例的某些数据有所不同。第 18.4.2 节将介绍实例化的使用。

三角形网格由一组顶点表示，每个顶点都对应模型表面上的一个特定位置。除了位置，每个顶点还可以关联其他可选属性，例如颜色或纹理坐标。表面法线也定义在网格顶点处，这种选择可能显得有些奇怪。从数学上说，每个三角形都有明确的表面法线，直接使用三角形的法线进行着色似乎更合理。然而，在渲染时，三角形网格通常用于表示其所近似的一个曲面，而顶点法线用于表示这个曲面的朝向，而非三角形网格本身的朝向。第 16.3.4 节将讨论计算顶点法线的方法。图 3.7 展示了两个表示曲面的三角形网格的侧视图，其中一个曲面平滑，另一个则带有一道尖锐的折痕。


![图 3.7 表示平滑曲面与折痕的三角形网格及顶点法线](https://raw.githubusercontent.com/ahuibo/Real-Time-Rendering-4th-CN/main/Real-Time_Rendering_4th_%E4%B8%AD%E6%96%87/assets/fig_3.5_3.7.png)

**图 3.7** 表示曲面（红色）的三角形网格侧视图（黑色，并绘有顶点法线）。左图使用平滑处理后的顶点法线来表示一个平滑曲面。右图将中间顶点复制了一份，并为其赋予两条法线，以表示一道折痕。

顶点着色器是处理三角形网格的第一个阶段。顶点着色器无法获取描述顶点如何构成三角形的数据。顾名思义，它只处理传入的顶点。顶点着色器提供了一种方式，可以修改、创建或忽略与各个三角形顶点关联的值，例如颜色、法线、纹理坐标和位置。通常，顶点着色器程序会将顶点从模型空间变换到齐次裁剪空间（第 4.7 节）。顶点着色器至少必须始终输出这个位置。

顶点着色器与前面描述的统一着色器大体相同。传入的每个顶点都由顶点着色器程序处理，随后程序输出若干值，这些值会在三角形或线段上进行插值。顶点着色器既不能创建顶点，也不能销毁顶点；处理一个顶点所产生的结果也不能传递给另一个顶点。由于各个顶点都被独立处理，因此可以使用 GPU 上任意数量的着色器处理器，并行处理传入的顶点流。

输入装配通常被描述为在顶点着色器执行之前发生的过程。这是物理模型往往不同于逻辑模型的一个例子。在实际物理实现中，为创建顶点而进行的数据读取可能发生在顶点着色器内部；驱动程序会悄然在每个着色器的开头添加适当的指令，而程序员看不到这些指令。

后续章节将解释几种顶点着色器效果，例如用于关节动画的顶点混合，以及轮廓渲染。顶点着色器的其他用途包括：

- 物体生成：只创建一次网格，再由顶点着色器使其变形。
- 使用蒙皮和变形技术，为角色的身体和面部制作动画。
- 程序化变形，例如旗帜、布料或水的运动 [802, 943]。
- 粒子创建：将退化的（没有面积的）网格送入流水线，再按需要使其具有面积。
- 镜头畸变、热浪扰动、水波纹、翻页卷曲以及其他效果：将整个帧缓冲区的内容作为纹理，贴在一个与屏幕对齐并进行程序化变形的网格上。
- 使用顶点纹理读取来应用地形高度场 [40, 1227]。

图 3.8 展示了使用顶点着色器实现的一些变形。


![图 3.8 顶点着色器实现的茶壶错切与噪声变形](https://raw.githubusercontent.com/ahuibo/Real-Time-Rendering-4th-CN/main/Real-Time_Rendering_4th_%E4%B8%AD%E6%96%87/assets/fig_3.5_3.8.png)

**图 3.8** 左图是一个正常的茶壶。顶点着色器程序执行一次简单的错切操作，便得到中间的图像。右图中，一个噪声函数生成了使模型发生扭曲的场。（图像使用 FX Composer 2 生成，由 NVIDIA 公司提供。）

顶点着色器的输出可以通过几种不同的方式被使用。通常的路径是：接下来生成每个实例的图元（例如三角形）并对其进行光栅化，再将产生的各个像素片元送入像素着色器程序，继续处理。在某些 GPU 上，这些数据也可以被送入曲面细分阶段或几何着色器，或者存入内存。以下各节将讨论这些可选阶段。


## 3.6 曲面细分阶段

原书来源：书页 44—46（PDF 物理页 65—67）。

曲面细分阶段使我们能够渲染曲面。GPU 的任务是接收每个曲面的描述，并将其转换为一组三角形来表示该曲面。这个阶段是一项可选的 GPU 功能，最早在 DirectX 11 中出现（并且是 DirectX 11 所要求的功能）。OpenGL 4.0 和 OpenGL ES 3.2 也支持它。

使用曲面细分阶段有几个优点。与直接提供相应的三角形相比，曲面的描述通常更加紧凑。除了节省内存之外，对于形状逐帧变化的动画角色或物体，这项功能还能避免 CPU 与 GPU 之间的总线成为瓶颈。通过针对给定视图生成适当数量的三角形，可以高效地渲染曲面。例如，如果一个球离相机很远，只需要少量三角形；而近距离观察时，使用数千个三角形来表示它，视觉效果可能最好。这种控制细节层次的能力，也使应用程序能够控制自身的性能，例如在性能较弱的 GPU 上使用质量较低的网格，以维持帧率。通常用平面表面表示的模型，可以转换为精细的三角形网格，然后按需要使其变形 [1493]；也可以对它们进行曲面细分，以降低执行高开销着色计算的频率 [225]。

曲面细分阶段始终由三个组成部分构成。采用 DirectX 的术语，它们分别是外壳着色器（hull shader）、曲面细分器（tessellator）和域着色器（domain shader）。在 OpenGL 中，外壳着色器称为曲面细分控制着色器（tessellation control shader），域着色器称为曲面细分求值着色器（tessellation evaluation shader）。这些名称虽然较为冗长，但描述性更强。在 OpenGL 中，固定功能的曲面细分器称为图元生成器（primitive generator）；正如后文所示，这确实就是它所做的工作。

如何描述曲线和曲面，以及如何对它们进行细分，将在第 17 章中详细讨论。这里简要介绍曲面细分各阶段的用途。首先，外壳着色器的输入是一种特殊的曲面片（patch）图元。它由若干控制点组成，这些控制点定义一个细分曲面、Bézier 曲面片，或其他类型的曲面元素。外壳着色器有两个作用。第一，告诉曲面细分器应该生成多少个三角形，以及采用何种配置。第二，对每个控制点进行处理。此外，外壳着色器还可以选择修改传入的曲面片描述，按需要添加或删除控制点。外壳着色器将其控制点集连同曲面细分控制数据一起输出给域着色器。见图 3.9。


![图 3.9 曲面细分阶段](https://raw.githubusercontent.com/ahuibo/Real-Time-Rendering-4th-CN/main/Real-Time_Rendering_4th_%E4%B8%AD%E6%96%87/assets/fig_3.6_3.9.png)

**图 3.9** 曲面细分阶段。外壳着色器接收由控制点定义的曲面片，并将曲面细分因子（TF）及类型发送给固定功能的曲面细分器。外壳着色器根据需要对控制点集进行变换，再将其连同曲面细分因子和相关的曲面片常量一起发送给域着色器。曲面细分器创建顶点集及其重心坐标。随后，域着色器处理这些数据，生成三角形网格（图中显示控制点以供参考）。

曲面细分器是流水线中的一个固定功能阶段，仅与曲面细分着色器配合使用。它的任务是添加若干新顶点，供域着色器处理。外壳着色器向曲面细分器发送所需细分曲面类型的信息：三角形、四边形或等参线（isoline）。等参线是一组线条带，有时用于头发渲染 [1954]。外壳着色器发送的另一类重要数值是曲面细分因子（在 OpenGL 中称为曲面细分级别，tessellation levels）。它们分为两类：内部因子和外边缘因子。两个内部因子决定三角形或四边形内部的细分程度。外部因子决定每条外边缘的分割程度（第 17.6 节）。图 3.10 展示了增大曲面细分因子的示例。通过允许分别控制这些因子，无论内部如何细分，都可以使相邻曲面的边缘在细分上相互匹配。边缘匹配可以避免曲面片交接处出现裂缝或其他着色瑕疵。顶点会被赋予重心坐标（第 22.8 节），这些数值指定所需曲面上每个点的相对位置。


![图 3.10 改变曲面细分因子的效果](https://raw.githubusercontent.com/ahuibo/Real-Time-Rendering-4th-CN/main/Real-Time_Rendering_4th_%E4%B8%AD%E6%96%87/assets/fig_3.6_3.10.png)

**图 3.10** 改变曲面细分因子的效果。犹他茶壶由 32 个曲面片组成。从左到右，内部和外部曲面细分因子分别为 1、2、4 和 8。（图像由 Rideout 和 Van Gelder 的演示程序生成 [1493]。）

外壳着色器始终输出一个曲面片，也就是一组控制点的位置。不过，它可以向曲面细分器发送一个小于或等于零的外部曲面细分级别（或非数值 NaN），以示意应丢弃该曲面片。否则，曲面细分器会生成一个网格，并将其发送给域着色器。每次调用域着色器时，都会使用外壳着色器提供的曲面控制点，计算每个顶点的输出值。域着色器的数据流模式与顶点着色器相似：对曲面细分器提供的每个输入顶点进行处理，并生成一个对应的输出顶点。所形成的三角形随后被传递到流水线的后续阶段。

虽然这套系统听起来很复杂，但它之所以采用这样的结构，是出于效率的考虑，而且每个着色器都可以相当简单。传入外壳着色器的曲面片通常只需很少的修改，甚至完全不需要修改。这个着色器还可以根据曲面片的估计距离或屏幕尺寸，动态计算曲面细分因子，例如用于地形渲染 [466]。另一种做法是，外壳着色器仅传递应用程序计算并提供的一组固定值，所有曲面片都使用这组值。曲面细分器执行一个复杂但固定功能的过程：生成顶点、赋予它们位置，并指定这些顶点组成哪些三角形或线段。为了提高计算效率，这一步数据扩增操作在着色器之外执行 [530]。域着色器取得为每个点生成的重心坐标，并将其用于曲面片的求值方程，以生成位置、法线、纹理坐标以及其他所需的顶点信息。图 3.11 给出了一个示例。


![图 3.11 使用 PN 三角形细分进行曲面细分和位移](https://raw.githubusercontent.com/ahuibo/Real-Time-Rendering-4th-CN/main/Real-Time_Rendering_4th_%E4%B8%AD%E6%96%87/assets/fig_3.6_3.11.png)

**图 3.11** 左图是由大约 6000 个三角形组成的基础网格。右图对每个三角形使用 PN 三角形细分，进行曲面细分和位移。（图像来自 NVIDIA SDK 11 [1301] 示例，由 NVIDIA 公司提供；模型来自 4A Games 的《地铁 2033》。）


## 3.7 几何着色器

来源：原书第 47—49 页（PDF 物理页第 68—70 页）。

几何着色器能够把图元转换为其他图元，这是曲面细分阶段做不到的。例如，可以让每个三角形生成表示其边的线段，从而将三角形网格转换为线框视图。另一种做法是用面向观察者的四边形替换这些线段，由此渲染出边线更粗的线框 [1492]。几何着色器于 2006 年末随 DirectX 10 的发布加入硬件加速的图形流水线。它在流水线中位于曲面细分着色器之后，可以选择是否使用。虽然几何着色器是 Shader Model 4.0 的必备组成部分，但更早的着色器模型并不使用它。OpenGL 3.2 和 OpenGL ES 3.2 也支持这种着色器。

几何着色器的输入是单个对象及其关联顶点。该对象通常由三角形条带中的三角形、一条线段，或者仅仅一个点构成。也可以定义扩展图元，并由几何着色器处理。具体来说，可以额外传入三角形外部的三个顶点，也可以使用折线上的两个相邻顶点。见图 3.12。在 DirectX 11 和 Shader Model 5.0 中，可以传入更复杂的面片，最多含有 32 个控制点。话虽如此，对于面片生成而言，曲面细分阶段的效率更高 [175]。


![图 3.12：几何着色器的输入图元](https://raw.githubusercontent.com/ahuibo/Real-Time-Rendering-4th-CN/main/Real-Time_Rendering_4th_%E4%B8%AD%E6%96%87/assets/fig_3.7_3.12.png)

**图 3.12** 几何着色器程序的输入属于某一种单一类型：点、线段或三角形。最右边的两个图元还包含与线段对象和三角形对象相邻的顶点。也可以使用更复杂的面片类型。

几何着色器处理这个图元，并输出零个或多个顶点；这些顶点被视为点、折线或三角形条带。注意，几何着色器也可以完全不产生输出。这样，就可以通过编辑顶点、添加新图元以及移除其他图元，有选择地修改网格。

几何着色器的设计用途是修改传入的数据，或者生成数量有限的副本。例如，一种用途是生成数据经过变换后的六份副本，从而同时渲染立方体贴图的六个面；见第 10.4.3 节。它也可以高效地创建级联阴影贴图，以生成高质量的阴影。其他利用几何着色器的算法包括：根据点数据创建大小可变的粒子，沿轮廓挤出鳍片以渲染毛皮，以及为阴影算法寻找对象的边缘。更多示例见图 3.13。本书后续内容将讨论这些用途以及其他用途。


![图 3.13：几何着色器的应用示例](https://raw.githubusercontent.com/ahuibo/Real-Time-Rendering-4th-CN/main/Real-Time_Rendering_4th_%E4%B8%AD%E6%96%87/assets/fig_3.7_3.13.png)

**图 3.13** 几何着色器（GS）的一些用途。左图利用 GS 即时对元球（metaball）等值面进行曲面细分。中图利用 GS 和流输出对线段进行分形细分，并由 GS 生成公告板来显示闪电。右图利用顶点着色器、几何着色器和流输出进行布料模拟。（图片来自 NVIDIA SDK 10 [1300] 的示例，由 NVIDIA 公司提供。）

DirectX 11 增加了几何着色器使用实例化的能力，使几何着色器可以对任意给定图元运行指定次数 [530, 1971]。在 OpenGL 4.0 中，这是通过调用次数来指定的。几何着色器还可以输出最多四条*流*。其中一条流可以继续沿渲染流水线向下传递，接受进一步处理。所有这些流都可以选择发送到流输出渲染目标。

几何着色器保证按照图元输入的顺序输出其结果。这会影响性能，因为如果多个着色器核心并行运行，就必须保存结果并按序排列。这一点以及其他因素，使得几何着色器不适合在单次调用中复制或创建大量几何数据 [175, 530]。

发出绘制调用之后，流水线中只有三个地方能够在 GPU 上产生工作：光栅化、曲面细分阶段和几何着色器。其中，从所需资源和内存的角度来看，几何着色器的行为最难预测，因为它是完全可编程的。实际上，几何着色器通常很少被使用，因为它不能很好地发挥 GPU 的优势。在某些移动设备上，几何着色器通过软件实现，因此明确不建议在这些设备上使用它 [69]。

### 3.7.1 流输出

GPU 流水线的标准用法是将数据送入顶点着色器，然后对得到的三角形进行光栅化，再在像素着色器中处理它们。过去，数据总是一路通过流水线，无法访问中间结果。Shader Model 4.0 引入了*流输出*的概念。顶点经顶点着色器处理之后（也可以选择再由曲面细分着色器和几何着色器处理），除了可以继续送往光栅化阶段，还可以作为一条流，也就是一个有序数组输出。事实上，可以完全关闭光栅化，使流水线纯粹作为非图形用途的流处理器使用。以这种方式处理后的数据可以重新送入流水线，从而实现迭代处理。这类操作可用于模拟流水或其他粒子效果，相关内容将在第 13.8 节讨论。它也可以用于对模型进行蒙皮，随后让这些顶点可供重复使用（第 4.4 节）。

流输出只能以浮点数的形式返回数据，因此可能产生相当明显的内存开销。流输出作用于图元，而不直接作用于顶点。如果将网格送入流水线，每个三角形都会生成自己的一组三个输出顶点。原始网格中的所有顶点共享关系都会丢失。因此，更典型的用法是仅将顶点作为点集图元送入流水线。在 OpenGL 中，流输出阶段称为*变换反馈*，因为它的许多用途主要是变换顶点，然后将顶点返回，以便进一步处理。图元保证按照输入顺序发送到流输出目标，这意味着顶点顺序将得到保持 [530]。


## 3.8 像素着色器

来源：*Real-Time Rendering, Fourth Edition*，书页 49—52（PDF 第 70—73 页）。

在顶点着色器、曲面细分着色器和几何着色器完成各自的操作后，图元会被裁剪，并完成光栅化的准备工作，如上一章所述。流水线这一部分的处理步骤相对固定，也就是说，它不可编程，但在一定程度上可以配置。系统会遍历每个三角形，以确定它覆盖了哪些像素。光栅器还可能粗略计算三角形覆盖每个像素单元面积的比例（第 5.4.2 节）。三角形中与像素部分或完全重叠的这一块称为**片元**（fragment）。

三角形顶点处的各个值，包括用于 z 缓冲的 z 值，都会沿三角形表面为每个像素进行插值。这些值被传给**像素着色器**（pixel shader），再由它处理片元。在 OpenGL 中，像素着色器称为**片元着色器**（fragment shader），这或许是更合适的名称。为保持一致，本书始终使用“像素着色器”这一名称。传入流水线的点图元和线图元也会为它们覆盖的像素产生片元。

三角形上的插值类型由像素着色器程序指定。通常，我们使用透视校正插值，使得随着物体向远处退去，各像素对应的表面位置之间的世界空间距离逐渐增大。一个例子是渲染延伸至地平线的铁轨。铁轨越远，枕木的间距看起来就越小，因为越接近地平线，每前进一个像素所跨越的距离就越大。还可以选择其他插值方式，例如不考虑透视投影的屏幕空间插值。DirectX 11 对何时以及如何进行插值提供了进一步的控制 [530]。

从编程角度看，顶点着色器程序的输出经过三角形（或线）上的插值后，实际上就成为像素着色器程序的输入。随着 GPU 的发展，又有其他输入被开放出来。例如，在 Shader Model 3.0 及以后的版本中，像素着色器可以获得片元的屏幕位置。此外，还有一个输入标志指示三角形的哪一面可见。在单个遍次中为每个三角形的正面和背面渲染不同材质时，这一信息很重要。

有了这些输入，像素着色器通常会计算并输出片元的颜色。它还可能生成不透明度值，并可选择修改片元的 z 深度。在合并过程中，这些值用于修改像素处存储的内容。光栅化阶段生成的深度值也可以由像素着色器修改。模板缓冲的值通常不可修改，而是直接传递到合并阶段。DirectX 11.3 允许着色器改变这个值。在 SM 4.0 中，雾计算和 alpha 测试等操作已从合并操作转为像素着色器中的计算 [175]。

像素着色器还具有一种独特的能力：丢弃传入的片元，也就是不产生任何输出。图 3.14 展示了片元丢弃的一种用法。裁剪平面功能最初是固定功能流水线中的一个可配置部分，后来改为在顶点着色器中指定。有了丢弃片元的能力之后，就可以在像素着色器中按任意所需方式实现这一功能，例如决定应当对各裁剪体执行逻辑与（AND）还是逻辑或（OR）运算。


![图 3.14 用户定义的裁剪平面](https://raw.githubusercontent.com/ahuibo/Real-Time-Rendering-4th-CN/main/Real-Time_Rendering_4th_%E4%B8%AD%E6%96%87/assets/fig_3_8_3.14.png)

**图 3.14.** 用户定义的裁剪平面。左图中，一个水平裁剪平面切开了物体。中图中，嵌套的球体被三个平面裁剪。右图中，只有当球体表面位于所有三个裁剪平面之外时，才会被裁剪。（出自 three.js 示例 `webgl_clipping` 和 `webgl_clipping_intersection` [218]。）

最初，像素着色器只能向合并阶段输出，以便最终显示。随着时间推移，像素着色器能够执行的指令数量显著增长。这一增长催生了**多渲染目标**（multiple render targets，MRT）的概念。像素着色器程序的结果不再仅仅送入颜色缓冲和 z 缓冲，而是可以为每个片元生成多组值，并将它们保存到不同的缓冲中，每个这样的缓冲称为一个**渲染目标**。各渲染目标通常具有相同的 x 和 y 尺寸；某些 API 允许尺寸不同，但渲染区域将取其中最小的范围。有些架构要求各渲染目标具有相同的位深，甚至可能要求数据格式也完全相同。可用渲染目标的数量取决于 GPU，为四个或八个。

即使存在这些限制，MRT 功能仍是更高效地执行渲染算法的有力帮助。单个渲染遍次就可以在一个目标中生成彩色图像，在另一个目标中生成物体标识符，并在第三个目标中生成世界空间距离。这一能力还催生了一种不同类型的渲染流水线，称为**延迟着色**（deferred shading），其中可见性判定和着色在不同的遍次中进行。第一个遍次在每个像素处存储物体的位置和材质数据，后续遍次便能高效地应用光照及其他效果。第 20.1 节介绍了这一类渲染方法。

像素着色器的限制在于，它通常只能在传给它的片元位置向渲染目标写入，并且不能读取相邻像素的当前结果。也就是说，像素着色器程序执行时，既不能把输出直接送到相邻像素，也不能访问其他像素刚刚发生的改变。它计算出的结果只影响它自己的像素。不过，这一限制并不像听起来那么严重。在一个遍次中生成的输出图像，其任意数据都可以在后续遍次中由像素着色器访问。相邻像素可以使用第 12.1 节介绍的图像处理技术来处理。

像素着色器无法得知或影响相邻像素结果的这条规则也有例外。其中一种是，在计算梯度或导数信息时，像素着色器可以立即访问相邻片元的信息，尽管这种访问是间接的。像素着色器可以获得任意插值值沿屏幕 x 轴和 y 轴每移动一个像素时的变化量。这些值对各种计算和纹理寻址都很有用。对于纹理过滤（第 6.2.2 节）等操作，这些梯度尤其重要，因为我们希望知道图像中多大的一块区域覆盖一个像素。所有现代 GPU 都通过以 2×2 为一组处理片元来实现这一功能，这样的一组称为一个 **quad（四像素组）**。当像素着色器请求一个梯度值时，返回的就是相邻片元之间的差值，见图 3.15。统一核心具有访问相邻数据的能力，这些数据保存在同一线程束（warp）的不同线程中，因此它可以计算供像素着色器使用的梯度。这种实现方式带来的一个结果是：在着色器中受动态流程控制影响的部分，即 `if` 语句或迭代次数可变的循环内，无法访问梯度信息。同一组中的所有片元都必须使用同一套指令进行处理，这样四个像素的结果才对梯度计算有意义。这是一项根本性的限制，即使在离线渲染系统中也存在 [64]。


![图 3.15 四像素组及梯度计算](https://raw.githubusercontent.com/ahuibo/Real-Time-Rendering-4th-CN/main/Real-Time_Rendering_4th_%E4%B8%AD%E6%96%87/assets/fig_3_8_3.15.png)

**图 3.15.** 左图中，一个三角形被光栅化为若干四像素组，每组包含 2×2 个像素。右图展示了以黑点标出的那个像素的梯度计算。四像素组中四个像素位置各自的 v 值均已标出。注意，其中三个像素并未被三角形覆盖，但 GPU 仍然会处理它们，以便求出梯度。利用左下角像素在四像素组中的两个相邻像素，可以计算它沿屏幕 x 和 y 方向的梯度。

DirectX 11 引入了一种允许向任意位置写入的缓冲类型，即**无序访问视图**（unordered access view，UAV）。最初只有像素着色器和计算着色器能够使用 UAV，DirectX 11.1 则把 UAV 的访问能力扩展到了所有着色器 [146]。OpenGL 4.3 将其称为**着色器存储缓冲对象**（shader storage buffer object，SSBO）。这两个名称分别从各自的角度描述了它的特点。像素着色器以任意顺序并行运行，并共享这一存储缓冲。

通常需要某种机制来避免**数据竞争条件**（data race condition，也称为**数据冒险**，data hazard）：两个着色器程序“争抢”着影响同一个值，可能导致不确定的结果。例如，如果像素着色器的两次调用几乎同时尝试对取出的同一个值进行加法操作，就可能发生错误。两者都会取出原始值，并分别在本地修改它，但最后写入结果的那次调用会抹去另一次调用的贡献，最终只执行了一次加法。GPU 通过提供着色器可以访问的专用原子操作单元来避免这一问题 [530]。不过，原子操作意味着某些着色器可能会停顿，因为它们需要等待访问某个内存位置，而另一个着色器正在对该位置执行读取、修改和写入。

虽然原子操作可以避免数据冒险，但许多算法要求特定的执行顺序。例如，你可能希望先绘制较远的蓝色透明三角形，然后再在其上叠加红色透明三角形，将红色混合到蓝色之上。对于一个像素，可能有两次像素着色器调用，每个三角形各对应一次，而它们的执行方式可能使红色三角形的着色器先于蓝色三角形的着色器完成。在标准流水线中，片元结果会先在合并阶段排序，然后再被处理。DirectX 11.3 引入了**光栅器顺序视图**（rasterizer order views，ROV），以强制保证执行顺序。它们类似于 UAV，着色器可以用相同的方式对其进行读写。关键区别在于，ROV 保证按照正确的顺序访问数据。这大大提高了这些着色器可访问缓冲的实用性 [327, 328]。例如，ROV 使像素着色器能够编写自己的混合方法，因为像素着色器可以直接访问并写入 ROV 中的任意位置，因此不再需要合并阶段 [176]。其代价是，如果检测到乱序访问，某次像素着色器调用可能会停顿，直到较早绘制的三角形处理完毕。


## 3.9 合并阶段

来源：原书第 53 页（PDF 第 74 页）；已核对 PDF 第 75 页的下一节边界。

如第 2.5.2 节所述，合并阶段负责将各个片元（在像素着色器中生成）的深度和颜色与帧缓冲中的内容合并。DirectX 将这一阶段称为**输出合并器**（output merger）；OpenGL 则将其称为**逐采样操作**（per-sample operations）。在大多数传统流水线示意图中（包括本书的示意图），模板缓冲和 z 缓冲操作都发生在这一阶段。如果片元可见，这一阶段还会执行颜色混合。对于不透明表面，并不涉及真正的混合，因为片元的颜色只是替换先前存储的颜色。将片元颜色与已存储颜色进行真正的混合，通常用于透明效果和合成操作（第 5.5 节）。

设想一个由光栅化生成的片元先经过像素着色器处理，随后在进行 z 缓冲测试时，却发现它被先前渲染的某个片元遮挡了。这样一来，像素着色器中完成的所有处理便都是多余的。为了避免这种浪费，许多 GPU 会在执行像素着色器之前先进行一些合并测试 [530]。测试使用片元的 z 深度（以及其他正在使用的功能，例如模板缓冲或剪裁测试）来判断可见性。如果片元被遮挡，就将其剔除。这种功能称为 **early-z**（提前深度测试）[1220, 1542]。像素着色器能够改变片元的 z 深度，也能够将片元完全丢弃。如果在像素着色器程序中发现了这两类操作中的任何一种，通常就无法使用 early-z，因此会将其关闭，这往往会降低流水线的效率。DirectX 11 和 OpenGL 4.2 允许像素着色器强制开启 early-z 测试，但存在若干限制 [530]。有关 early-z 和其他 z 缓冲优化的更多内容，见第 23.7 节。有效地使用 early-z 可以显著影响性能，第 18.4.5 节将对此进行详细讨论。

合并阶段处于固定功能阶段（例如三角形设置）与完全可编程的着色器阶段之间。虽然它不可编程，但其操作具有很高的可配置性。尤其是颜色混合，可以配置为执行大量不同的操作。最常见的是涉及颜色值和 alpha 值的乘法、加法与减法的组合，但也可以执行其他操作，例如取最小值、取最大值以及按位逻辑运算。DirectX 10 增加了一项能力，可以将像素着色器输出的两个颜色与帧缓冲颜色进行混合。这种能力称为**双源颜色混合**（dual source-color blending），不能与多渲染目标同时使用。除此之外，多渲染目标（MRT）本身支持混合，而 DirectX 10.1 又引入了对每个独立缓冲执行不同混合操作的能力。

如上一节末尾所述，DirectX 11.3 提供了一种通过光栅器有序视图（ROV）使混合可编程的方法，不过需要付出性能代价。ROV 和合并阶段都保证绘制顺序，也称为**输出不变性**（output invariance）。无论像素着色器的结果以什么顺序生成，API 都要求将这些结果按照输入时的顺序排序，再逐个物体、逐个三角形地发送至合并阶段。


## 3.10 计算着色器

来源：原书第 54—55 页（PDF 第 75—76 页）。

GPU 的用途不仅限于实现传统的图形流水线。在许多不同领域中，它还有大量非图形用途，例如计算股票期权的估计价值，以及训练用于深度学习的神经网络。以这种方式使用硬件称为 **GPU 计算**。CUDA 和 OpenCL 等平台把 GPU 当作大规模并行处理器来控制，并不真正需要图形专用功能，也不访问这些功能。这些框架通常使用带有扩展的 C 或 C++ 等语言，以及专为 GPU 编写的库。

DirectX 11 引入的**计算着色器**是 GPU 计算的一种形式，因为它是一种不固定在图形流水线某个位置的着色器。它通过图形 API 调用，因此与渲染过程紧密相关。它与顶点着色器、像素着色器以及其他着色器配合使用，并使用与流水线相同的统一着色器处理器资源池。它和其他着色器一样，具有某组输入数据，并且能够访问缓冲区（例如纹理）来进行输入和输出。在计算着色器中，线程束（warp）和线程更加显式。例如，每次调用都会获得一个可供自身访问的线程索引。此外，还有**线程组**这一概念；在 DirectX 11 中，一个线程组由 1 到 1024 个线程组成。这些线程组通过 x、y 和 z 坐标来指定，主要是为了方便在着色器代码中使用。每个线程组都有少量可在线程之间共享的内存。在 DirectX 11 中，这部分内存为 32 kB。计算着色器按线程组执行，从而保证组内的所有线程并发运行 [1971]。

计算着色器的一项重要优势是能够访问 GPU 上生成的数据。将数据从 GPU 发送到 CPU 会产生延迟，因此，如果能够让处理过程和结果都留在 GPU 上，就可以提高性能 [1403]。后处理，即以某种方式修改已经渲染好的图像，是计算着色器的一种常见用途。共享内存意味着，对图像像素进行采样所得到的中间结果，可以与相邻线程共享。例如，研究发现，使用计算着色器确定图像的亮度分布或平均亮度，其运行速度可达到使用像素着色器执行同一操作的两倍 [530]。

计算着色器还适用于粒子系统、面部动画等网格处理 [134]、剔除 [1883, 1884]、图像滤波 [1102, 1710]、提高深度精度 [991]、阴影 [865]、景深 [764]，以及其他任何能够利用一组 GPU 处理器来完成的任务。Wihlidal [1884] 讨论了计算着色器如何能够比曲面细分的外壳着色器更加高效。其他用途见图 3.16。

至此，我们对 GPU 如何实现渲染流水线的回顾就结束了。GPU 的功能可以通过多种方式使用和组合，以执行各种与渲染相关的处理。相关理论，以及为利用这些能力而调整的算法，是本书的核心主题。接下来，我们将把重点转向变换和着色。


![图 3.16：计算着色器示例，包括受风影响的头发、快速模糊和海浪模拟。](https://raw.githubusercontent.com/ahuibo/Real-Time-Rendering-4th-CN/main/Real-Time_Rendering_4th_%E4%B8%AD%E6%96%87/assets/fig_3.10_3.16.png)

**图 3.16** 计算着色器示例。左图使用计算着色器模拟受风影响的头发，而头发本身则使用曲面细分阶段进行渲染。中图由计算着色器执行快速模糊操作。右图模拟海浪。（图片来自 NVIDIA SDK 11 [1301] 示例，由 NVIDIA 公司提供。）


## 延伸阅读与资源

Giesen 的图形流水线导览 [530] 详细讨论了 GPU 的许多方面，解释各个组成部分为何以相应的方式工作。Fatahalian 和 Bryant 的课程 [462] 通过一系列详细的讲义幻灯片，讨论 GPU 并行性。Kirk 和 Hwa 的书 [903] 虽然着重介绍使用 CUDA 进行 GPU 计算，但其入门部分讨论了 GPU 的演进及设计思想。

系统学习着色器编程需要付出一些努力。《OpenGL Superbible》[1606] 和《OpenGL Programming Guide》[885] 等书都包含着色器编程内容。较早的《OpenGL Shading Language》[1512] 没有涉及几何着色器、曲面细分着色器等较新的着色器阶段，但它专门着重讨论与着色器有关的算法。较新的书籍和推荐读物，请参见本书网站 [realtimerendering.com](https://realtimerendering.com/)。
